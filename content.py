"""
Course content for the plain teaching app - no login, no storage, no file
exchange. Each chapter has three parts:
  notes    - theory, shown as bullet points
  practice - a numbered list of things to actually try in Excel on the
             learner's own computer, self-directed, never checked
  quiz     - multiple choice, 2 attempts per question, then the correct
             answer is revealed. Doesn't gate anything - "Done" always works.

Chapter dict shape:
{
  "id": "U1C1",
  "num": 1,
  "title": "...",
  "notes": ["bullet", ...],
  "practice": ["step 1", "step 2", ...],
  "quiz": [{"q": "...", "options": [...], "answer": 0}, ...],
}
"""

UNIT1_CHAPTERS = [
    dict(
        id="U1C1",
        num=1,
        title="What is Excel & What It's Used For",
        notes=[
            "Excel is a spreadsheet program: it stores information in a grid of rows and columns.",
            "Each small box in the grid is called a cell. A cell is named by its column letter and row number, e.g. B3.",
            "People use Excel to organize lists, do calculations automatically, track money, analyze data, and build charts.",
            "Real examples: a class marksheet, a monthly pocket-money tracker, a cricket team's scorecard, a school attendance register.",
            "The real power of Excel: change one number, and every total or chart connected to it updates by itself. Paper and plain text lists cannot do that.",
            "A single Excel file is called a workbook. A workbook can hold many worksheets (tabs) inside it.",
        ],
        practice=[
            "Open Excel and start a new blank workbook.",
            "In column B, type the names of 3 friends or classmates, one per row (B2, B3, B4).",
            "In column C, type a favorite subject for each. In column D, type a mark out of 100.",
            "Click on a few different cells and watch the Name Box (top-left) show you exactly which cell you're in, e.g. C3.",
            "Change one of the marks in column D. Notice nothing else updates yet - that's expected. You'll connect cells with formulas from Chapter 3 onward.",
        ],
        quiz=[
            dict(q="What do we call the small box where a row and a column meet?", options=["Cell", "Range", "Field", "Tab"], answer=0),
            dict(q="True or False: in Excel, changing one number can update related totals automatically (once formulas are involved).", options=["True", "False"], answer=0),
            dict(q="What do we call an Excel file that can hold many worksheets inside it?", options=["Workbook", "Worksheet", "Document", "Folder"], answer=0),
            dict(q="If you type a value into cell C3, what does the Name Box show while that cell is selected?", options=["C3", "Cell 3", "Row C", "3C"], answer=0),
        ],
    ),
    dict(
        id="U1C2",
        num=2,
        title="Interface Tour: Ribbon, Tabs, Quick Access Toolbar",
        notes=[
            "The Ribbon is the strip of buttons and icons across the top of Excel, organized into tabs.",
            "Common tabs: Home (formatting, fonts), Insert (charts, tables), Formulas (functions), Data (sorting, filtering), View (freeze panes, zoom).",
            "The Quick Access Toolbar sits above or below the Ribbon and holds shortcuts you use constantly, like Save and Undo.",
            "The Formula Bar (below the Ribbon) shows exactly what is typed in the selected cell, including formulas.",
            "The Name Box (left of the Formula Bar) shows which cell is currently selected, and lets you jump straight to a cell by typing its reference.",
            "The Status Bar at the bottom shows quick info like the sum or average of any cells you've selected.",
        ],
        practice=[
            "Open Excel (or switch to the workbook from Chapter 1).",
            "Find and point to: the Ribbon, the Formula Bar, the Name Box, the Status Bar.",
            "Click through the Home, Insert, and Formulas tabs on the Ribbon and notice the buttons change.",
            "Select a couple of number cells and look at the Status Bar - it shows their Sum and Average automatically.",
            "Type a cell reference like D10 into the Name Box and press Enter - it jumps straight there.",
        ],
        quiz=[
            dict(q="Which part of Excel shows exactly what you typed into the selected cell?", options=["Formula Bar", "Status Bar", "Name Box", "Ribbon"], answer=0),
            dict(q="Which tab would you open to insert a chart?", options=["Insert", "Home", "Data", "View"], answer=0),
            dict(q="Which part lets you type a cell reference to jump straight to it?", options=["Name Box", "Formula Bar", "Ribbon", "Status Bar"], answer=0),
            dict(q="Which bar at the bottom shows a quick sum of selected cells?", options=["Status Bar", "Formula Bar", "Ribbon", "Title Bar"], answer=0),
        ],
    ),
    dict(
        id="U1C3",
        num=3,
        title="Workbooks, Worksheets & File Structure",
        notes=[
            "A workbook is the entire Excel file (it ends in .xlsx). A worksheet is one tab inside that file.",
            "A new workbook opens with one worksheet by default, but you can add as many as you need.",
            "Worksheet tabs sit at the bottom of the window - click a tab to switch sheets, right-click for options like Rename, Delete, or Move.",
            "Sheets in one workbook can reference each other, e.g. a Summary sheet can pull totals from a Sales sheet.",
            "Saving: Ctrl+S saves the workbook. The default modern format is .xlsx; older files may be .xls; data-only exports often use .csv.",
        ],
        practice=[
            "In your workbook, click the + button next to the sheet tabs to add a new sheet.",
            "Right-click the new tab and choose Rename - call it 'Practice'.",
            "Right-click again and try Move or Copy to reorder your tabs.",
            "Press Ctrl+S to save. Notice the file extension Excel saves as by default.",
        ],
        quiz=[
            dict(q="What do we call the entire Excel file, which can contain many sheets?", options=["Workbook", "Worksheet", "Cell", "Range"], answer=0),
            dict(q="What do we call a single tab inside a workbook?", options=["Worksheet", "Workbook", "Window", "Panel"], answer=0),
            dict(q="What is the modern default file extension for an Excel workbook?", options=[".xlsx", ".xls", ".csv", ".doc"], answer=0),
            dict(q="Right-clicking a sheet tab lets you Rename, Delete, or do what else?", options=["Move", "Print", "Merge", "Sort"], answer=0),
        ],
    ),
    dict(
        id="U1C4",
        num=4,
        title="Navigating Cells, Rows, Columns & Ranges",
        notes=[
            "Every cell has an address: column letter first, then row number - e.g. C5 means column C, row 5.",
            "A range is a group of cells, written as start:end - e.g. B2:B6 means cells B2, B3, B4, B5, B6.",
            "Arrow keys move one cell at a time. Ctrl+Arrow jumps to the edge of a block of data.",
            "Ctrl+Home jumps to cell A1. Ctrl+End jumps to the last used cell in the sheet.",
            "Click a cell and drag to select a range, or click a column/row header to select the whole column/row.",
        ],
        practice=[
            "In your workbook, click cell A1, then press Ctrl+End - see where it jumps.",
            "Press Ctrl+Home to come back to A1.",
            "Click cell B2 and drag down to B6 to select the range B2:B6.",
            "Click a column header (like the letter C at the top) to select the whole column.",
        ],
        quiz=[
            dict(q="What is the cell reference for row 5, column C?", options=["C5", "5C", "R5C3", "C-5"], answer=0),
            dict(q="How many cells are in the range B2:B6?", options=["5", "4", "6", "3"], answer=0),
            dict(q="Which keyboard shortcut jumps straight to cell A1?", options=["Ctrl+Home", "Ctrl+End", "Ctrl+Arrow", "Ctrl+Tab"], answer=0),
            dict(q="Which key combination jumps to the edge of a block of data?", options=["Ctrl+Arrow", "Ctrl+Shift+Arrow", "Ctrl+Home", "Alt+Arrow"], answer=0),
        ],
    ),
    dict(
        id="U1C5",
        num=5,
        title="Entering & Editing Data",
        notes=[
            "Click a cell and start typing to enter data. Press Enter to confirm and move down, or Tab to confirm and move right.",
            "Press F2 (or double-click a cell) to edit its existing content instead of replacing it.",
            "Press Esc to cancel an edit before confirming it.",
            "AutoComplete: if a column already has text like 'Mumbai', typing 'Mu' again will suggest it - press Enter to accept or keep typing to override.",
            "Numbers align right by default, text aligns left - a quick visual check that Excel read your entry as the type you intended.",
        ],
        practice=[
            "Type a value into a blank cell and press Tab instead of Enter - notice which direction the cursor moves.",
            "Press F2 on a cell that already has text in it, and edit it without retyping the whole thing.",
            "Start editing a cell, then press Esc - confirm your original value is unchanged.",
            "Type the same city name into two cells in the same column and watch AutoComplete suggest it the second time.",
        ],
        quiz=[
            dict(q="Which key lets you edit a cell's existing content instead of replacing it?", options=["F2", "F1", "Esc", "Tab"], answer=0),
            dict(q="Which key cancels an edit before you confirm it?", options=["Esc", "F2", "Delete", "Backspace"], answer=0),
            dict(q="Pressing Tab after typing a value moves the cursor which direction?", options=["Right", "Down", "Left", "Up"], answer=0),
            dict(q="By default, do numbers align left or right in a cell?", options=["Right", "Left", "Center", "Justified"], answer=0),
        ],
    ),
    dict(
        id="U1C6",
        num=6,
        title="Copy, Cut, Paste & Paste Special",
        notes=[
            "Ctrl+C copies a cell or range (keeps the original in place). Ctrl+X cuts it (removes the original once pasted).",
            "Ctrl+V pastes everything: value, formula, and formatting.",
            "Paste Special (Ctrl+Alt+V) lets you paste only what you want - e.g. Values Only (no formulas), or Formats Only (no data).",
            "Values Only paste is very common: it 'freezes' a formula's result as a plain number.",
            "Dragging the small square at a cell's bottom-right corner (the Fill Handle) copies a value or pattern into neighboring cells.",
        ],
        practice=[
            "Copy a cell with Ctrl+C, click a different cell, and paste it with Ctrl+V.",
            "Try Ctrl+X on a different cell, then paste it elsewhere - notice the original disappears.",
            "Copy a cell, then use Ctrl+Alt+V (Paste Special) and choose Values Only somewhere else.",
            "Type a value, then drag the small square at the cell's bottom-right corner (the Fill Handle) down a few rows.",
        ],
        quiz=[
            dict(q="Which shortcut copies a cell while keeping the original in place?", options=["Ctrl+C", "Ctrl+X", "Ctrl+V", "Ctrl+Z"], answer=0),
            dict(q="Which shortcut removes the original once pasted elsewhere?", options=["Ctrl+X", "Ctrl+C", "Ctrl+V", "Ctrl+D"], answer=0),
            dict(q="Which Paste Special option 'freezes' a formula's result as a plain number?", options=["Values Only", "Formats Only", "Formulas Only", "All"], answer=0),
            dict(q="What is the small square at a cell's bottom-right corner called?", options=["Fill Handle", "Selection Box", "Anchor Point", "Grip"], answer=0),
        ],
    ),
    dict(
        id="U1C7",
        num=7,
        title="Undo/Redo, AutoSave & File Formats",
        notes=[
            "Ctrl+Z undoes your last action. Ctrl+Y (or Ctrl+Shift+Z) redoes an action you just undid.",
            "Excel keeps a long undo history in one session, so you can usually step back through many changes.",
            "AutoSave (when a file is stored on OneDrive/SharePoint) saves your changes automatically every few seconds.",
            "Without AutoSave, save manually and often with Ctrl+S - unsaved work is lost if Excel closes unexpectedly.",
            ".xlsx is the standard modern Excel format. .xls is the older format (limited rows/features). .csv stores plain data only - no formulas, no formatting, no multiple sheets.",
        ],
        practice=[
            "Make a change to any cell, undo it with Ctrl+Z, then redo it with Ctrl+Y.",
            "Open File > Save As and look at the list of file type options - find .xlsx and .csv.",
            "If you have OneDrive, check whether AutoSave is toggled on at the top-left of the Excel window.",
        ],
        quiz=[
            dict(q="Which shortcut undoes your last action?", options=["Ctrl+Z", "Ctrl+Y", "Ctrl+X", "Ctrl+U"], answer=0),
            dict(q="Which shortcut redoes an action you just undid?", options=["Ctrl+Y", "Ctrl+Z", "Ctrl+R", "Ctrl+D"], answer=0),
            dict(q="Which file format stores plain data only, no formulas or formatting?", options=[".csv", ".xlsx", ".xls", ".xltx"], answer=0),
            dict(q="Where must a file be stored for AutoSave to work automatically?", options=["OneDrive/SharePoint", "Desktop", "USB Drive", "Downloads folder"], answer=0),
        ],
    ),
    dict(
        id="U1C8",
        num=8,
        title="Essential Keyboard Shortcuts",
        notes=[
            "Ctrl+S = Save. Ctrl+P = Print. Ctrl+N = New workbook.",
            "Ctrl+B = Bold. Ctrl+I = Italic. Ctrl+U = Underline.",
            "Ctrl+1 = Open Format Cells dialog.",
            "Ctrl+Arrow (any direction) = jump to the edge of a data block in that direction.",
            "Ctrl+Shift+Arrow = select from the current cell to the edge of a data block.",
            "Alt+= = AutoSum: instantly inserts a SUM formula for the numbers above or beside the cell.",
            "Ctrl+Home = jump to A1. Ctrl+End = jump to the last used cell.",
        ],
        practice=[
            "On your practice sheet, select a cell and press Ctrl+1 to open Format Cells - look around, then close it.",
            "Type a few numbers in a column, click the cell below them, and press Alt+= to AutoSum them.",
            "Select some text and press Ctrl+B to bold it.",
            "Select a cell inside a block of data and press Ctrl+Shift+Arrow to select to the edge of that block.",
        ],
        quiz=[
            dict(q="Which shortcut opens the Format Cells dialog?", options=["Ctrl+1", "Ctrl+2", "Ctrl+F", "Alt+1"], answer=0),
            dict(q="Which shortcut instantly inserts a SUM formula (AutoSum)?", options=["Alt+=", "Ctrl+=", "Ctrl+Shift+S", "Alt+S"], answer=0),
            dict(q="Which shortcut makes selected text Bold?", options=["Ctrl+B", "Ctrl+I", "Ctrl+U", "Ctrl+H"], answer=0),
            dict(q="Which shortcut selects to the edge of a data block?", options=["Ctrl+Shift+Arrow", "Ctrl+Arrow", "Shift+Arrow", "Alt+Arrow"], answer=0),
        ],
    ),
]

UNIT2_CHAPTERS = [
    dict(
        id="U2C1",
        num=1,
        title="Number Formats: General, Currency, Percentage & Date",
        notes=[
            "Formatting changes how a value looks on screen - it never changes the actual stored value or the result of any formula using it.",
            "The Number group on the Home tab holds the most common formats: General, Number, Currency, Accounting, Percentage, Date, and Text.",
            "Currency format adds a symbol (like Rs. or $) and usually 2 decimal places; Accounting format aligns symbols and decimals in a column, which looks tidier in money tables.",
            "Percentage format multiplies the displayed value by 100 and adds a % sign - the underlying stored number is still the decimal, e.g. 0.25 displays as 25%.",
            "Date format can display the same underlying date-serial-number many ways (31-01-2026, Jan 31 2026, 2026-01-31) - the value doesn't change, only the display.",
            "Ctrl+1 opens the Format Cells dialog, which has every format option plus custom format codes.",
        ],
        practice=[
            "Type the number 2500 in a cell and apply Currency format from the Home tab.",
            "Type 0.25 in another cell and apply Percentage format - confirm it displays as 25%.",
            "Type today's date in a cell, then press Ctrl+1 and try switching between two different date display styles.",
            "Type a number, apply Accounting format, then compare how it lines up versus Currency format in the column.",
        ],
        quiz=[
            dict(q="Does changing a cell's number format change the value stored in it?", options=["No, only the display changes", "Yes, it rounds the real value", "Yes, it converts it to text", "Only for percentages"], answer=0),
            dict(q="If a cell stores 0.25 and you apply Percentage format, what does it display?", options=["25%", "0.25%", "2.5%", "250%"], answer=0),
            dict(q="Which shortcut opens the Format Cells dialog?", options=["Ctrl+1", "Ctrl+2", "Ctrl+F", "Ctrl+Shift+1"], answer=0),
            dict(q="Which format aligns currency symbols and decimals neatly in a column?", options=["Accounting", "Currency", "General", "Percentage"], answer=0),
        ],
    ),
    dict(
        id="U2C2",
        num=2,
        title="Font, Fill, Borders & Cell Styles",
        notes=[
            "Font tools (Home tab) control typeface, size, bold, italic, underline, and font color for any selected cell or range.",
            "Fill Color shades a cell's background - useful for highlighting totals, headers, or flagged rows.",
            "Borders add visible lines around cells or ranges - useful for separating sections of a sheet, especially when printing.",
            "Cell Styles (Home tab, Styles group) are ready-made format combinations (like 'Heading 1' or 'Good/Bad/Neutral') that apply several formats at once and stay consistent across a workbook.",
            "Format Painter (the little paintbrush icon) copies all formatting from one cell and lets you 'paint' it onto other cells without retyping anything.",
        ],
        practice=[
            "Select a header row and make it bold, then apply a fill color to it.",
            "Add borders around a small range of data to box it in.",
            "Apply one of the built-in Cell Styles to a total row.",
            "Use Format Painter to copy the header row's formatting onto a different row.",
        ],
        quiz=[
            dict(q="What does Fill Color change on a cell?", options=["Its background shading", "Its text color", "Its border", "Its number format"], answer=0),
            dict(q="What does the Format Painter tool do?", options=["Copies formatting from one cell to another", "Copies only the value", "Copies only formulas", "Deletes formatting"], answer=0),
            dict(q="What are Cell Styles?", options=["Ready-made combinations of formats", "A type of chart", "A formula category", "A file format"], answer=0),
            dict(q="Where do you find Bold, Italic, and Underline?", options=["Home tab, Font group", "Insert tab", "Data tab", "View tab"], answer=0),
        ],
    ),
    dict(
        id="U2C3",
        num=3,
        title="Alignment, Wrap Text & Merge Cells",
        notes=[
            "Horizontal alignment (left/center/right) and vertical alignment (top/middle/bottom) are set from the Alignment group on the Home tab.",
            "Wrap Text makes long text inside a cell break onto multiple lines instead of overflowing or getting cut off, growing the row height automatically.",
            "Merge & Center combines several cells into one larger cell and centers the content - handy for titles spanning several columns.",
            "Merged cells can cause problems later with sorting, filtering, and formulas, since a merged cell only 'belongs' to its top-left original cell - use merging sparingly, mainly for titles/headers, not for data.",
            "Indent buttons nudge text right within a cell without changing alignment - useful for showing hierarchy, like sub-items under a category.",
        ],
        practice=[
            "Type a long sentence into a single cell and turn on Wrap Text to see it break into multiple lines.",
            "Select a range across several columns above your data and use Merge & Center to create a title.",
            "Try sorting a data range that includes a merged cell and notice the error Excel gives - then undo.",
            "Use the Indent button to nudge a sub-item cell to the right under its category.",
        ],
        quiz=[
            dict(q="What does Wrap Text do to long content in a cell?", options=["Breaks it onto multiple lines within the cell", "Shrinks the font automatically", "Deletes the overflow", "Moves it to the next cell"], answer=0),
            dict(q="What is a downside of merged cells mentioned in this chapter?", options=["They can cause problems with sorting and filtering", "They cannot hold numbers", "They cannot be colored", "They disable formulas everywhere"], answer=0),
            dict(q="What does Merge & Center do?", options=["Combines cells into one and centers the content", "Splits one cell into many", "Centers text without merging", "Aligns numbers only"], answer=0),
            dict(q="What does the Indent button do?", options=["Nudges text right within a cell", "Adds a border", "Wraps text", "Merges cells"], answer=0),
        ],
    ),
    dict(
        id="U2C4",
        num=4,
        title="Row Height, Column Width & Hiding Rows/Columns",
        notes=[
            "Drag the border between two column letters (or row numbers) to resize manually, or double-click that border to auto-fit the width/height to the content.",
            "Right-click a column or row header for a precise numeric width/height instead of dragging by eye.",
            "Select multiple column or row headers before dragging/right-clicking to resize them all at once, identically.",
            "Hiding a row or column (right-click > Hide) keeps its data intact but removes it from view and from printouts - Unhide brings it back.",
            "Hidden rows/columns are easy to forget about - if numbers in a sheet don't add up as expected, check for hidden rows/columns that might be excluded from a selection or included unexpectedly.",
        ],
        practice=[
            "Double-click the border between two column headers to auto-fit a column's width to its content.",
            "Select three columns at once and set their width to the same exact number via right-click.",
            "Hide a column, confirm its data is still there by unhiding it again.",
            "Select a range of rows including a hidden one, and notice how AutoSum/Status Bar totals treat it.",
        ],
        quiz=[
            dict(q="What happens when you double-click the border between two column headers?", options=["The column auto-fits to its content", "The column is deleted", "The column is hidden", "The column is merged"], answer=0),
            dict(q="Does hiding a row delete its data?", options=["No, the data stays, just not visible", "Yes, it is permanently deleted", "Only formulas are deleted", "Only formatting is deleted"], answer=0),
            dict(q="How do you resize several columns to the exact same width at once?", options=["Select them all, then set width via right-click", "There is no way to do this", "Resize one, then Ctrl+Z on others", "Use Format Painter only"], answer=0),
            dict(q="What is a common troubleshooting tip mentioned for numbers not adding up correctly?", options=["Check for hidden rows/columns", "Check the font color", "Check the file name", "Restart Excel"], answer=0),
        ],
    ),
    dict(
        id="U2C5",
        num=5,
        title="Conditional Formatting Basics",
        notes=[
            "Conditional Formatting (Home tab) automatically applies formatting - like color - to cells that meet a rule you set, and updates live as data changes.",
            "Common built-in rules: Highlight Cells Rules (e.g. 'Greater Than 50'), Top/Bottom Rules (e.g. 'Top 10 Items'), Color Scales, Data Bars, and Icon Sets.",
            "Color Scales shade cells on a gradient based on value (e.g. red for low, green for high) - useful for spotting patterns at a glance across a range.",
            "Data Bars draw a small in-cell bar proportional to the value, like a mini bar chart inside the cell itself.",
            "Manage Rules (in the Conditional Formatting menu) lets you view, edit, reorder, or delete every rule applied to a sheet.",
        ],
        practice=[
            "Select a column of numbers and apply 'Highlight Cells Rules > Greater Than' with a value of your choice.",
            "Apply a Color Scale to the same column and compare how it looks versus the highlight rule.",
            "Apply Data Bars to a column of numbers and observe the in-cell bars.",
            "Open Conditional Formatting > Manage Rules and delete one of the rules you just created.",
        ],
        quiz=[
            dict(q="Does Conditional Formatting update automatically when the underlying data changes?", options=["Yes", "No, it must be reapplied manually", "Only for Color Scales", "Only for numbers, not text"], answer=0),
            dict(q="What does a Data Bar show inside a cell?", options=["A mini bar proportional to the value", "A percentage only", "A color gradient", "An icon"], answer=0),
            dict(q="Where do you view, edit, or delete all conditional formatting rules on a sheet?", options=["Manage Rules", "Format Cells", "Cell Styles", "Page Layout"], answer=0),
            dict(q="Which built-in rule type would highlight the 10 highest values in a range?", options=["Top/Bottom Rules", "Highlight Cells Rules", "Icon Sets", "Data Bars"], answer=0),
        ],
    ),
    dict(
        id="U2C6",
        num=6,
        title="Freeze Panes, Split & Page Layout for Printing",
        notes=[
            "Freeze Panes (View tab) locks chosen rows/columns in place so they stay visible while you scroll through a large sheet - commonly used to freeze a header row.",
            "'Freeze Top Row' and 'Freeze First Column' are one-click shortcuts inside the Freeze Panes menu for the two most common cases.",
            "Split (View tab) divides the window into separate scrollable panes without locking them - useful for comparing two distant parts of the same sheet.",
            "Page Layout view and Print Preview (File > Print) show how a sheet will look on paper, including page breaks.",
            "Print Titles (Page Layout tab > Print Titles) repeats a chosen header row on every printed page - different from Freeze Panes, which is a screen-only feature.",
        ],
        practice=[
            "In a sheet with more rows than fit on screen, use View > Freeze Panes > Freeze Top Row, then scroll down.",
            "Try Split instead, and scroll the two panes independently.",
            "Open File > Print and look at the Print Preview of your sheet.",
            "Set Print Titles to repeat your header row on every page if the sheet were multi-page.",
        ],
        quiz=[
            dict(q="What does Freeze Top Row do?", options=["Keeps the top row visible while scrolling", "Locks all cells from editing", "Hides the top row", "Prints the top row only"], answer=0),
            dict(q="How is Split different from Freeze Panes?", options=["Split creates independently scrollable panes without locking them", "Split only works on printed pages", "Split freezes columns only", "Split is the same as Freeze Panes"], answer=0),
            dict(q="What does Print Titles do?", options=["Repeats a header row on every printed page", "Freezes a row on screen", "Adds a title to the file", "Changes the page orientation"], answer=0),
            dict(q="Where would you check how a sheet will look on paper before printing?", options=["Print Preview", "Freeze Panes", "Format Cells", "Name Box"], answer=0),
        ],
    ),
]

UNIT3_CHAPTERS = [
    dict(
        id="U3C1",
        num=1,
        title="Formula Syntax: =, Operators & Order of Operations",
        notes=[
            "Every formula starts with an equals sign (=). Without it, Excel treats your entry as plain text or a number, not something to calculate.",
            "Basic math operators: + (add), - (subtract), * (multiply), / (divide), ^ (exponent, e.g. power of 2).",
            "Excel follows the standard order of operations (PEMDAS): parentheses first, then exponents, then multiplication/division, then addition/subtraction, left to right.",
            "Use parentheses to force a calculation to happen in a specific order, e.g. =(2+3)*4 gives 20, while =2+3*4 gives 14.",
            "You can reference other cells inside a formula instead of typing raw numbers, e.g. =B2+B3 - this is what makes formulas update automatically when data changes.",
        ],
        practice=[
            "In a blank cell, type =2+3*4 and confirm the result follows order of operations.",
            "In another cell, type =(2+3)*4 and compare the result to the previous one.",
            "Put numbers in two cells, then in a third cell write a formula that adds them by referencing their cell addresses, not by retyping the numbers.",
            "Change one of the two number cells and watch the formula result update automatically.",
        ],
        quiz=[
            dict(q="What must every Excel formula begin with?", options=["=", "+", "@", "#"], answer=0),
            dict(q="What does =2+3*4 evaluate to, following order of operations?", options=["14", "20", "9", "24"], answer=0),
            dict(q="What does =(2+3)*4 evaluate to?", options=["20", "14", "9", "10"], answer=0),
            dict(q="Why reference cells (like =B2+B3) instead of typing raw numbers into a formula?", options=["The formula updates automatically when those cells change", "It runs faster", "It is required by Excel", "It changes the cell's format"], answer=0),
        ],
    ),
    dict(
        id="U3C2",
        num=2,
        title="Cell References: Relative, Absolute & Mixed",
        notes=[
            "A relative reference (like A1) shifts automatically when a formula is copied to a new location - this is the default behavior.",
            "An absolute reference (like $A$1) stays locked to that exact cell no matter where the formula is copied - the dollar signs 'anchor' it.",
            "Press F4 while a reference is selected in the formula bar to cycle through relative, absolute, and mixed reference styles quickly.",
            "A mixed reference locks only the column ($A1) or only the row (A$1), letting one part move and the other stay fixed when copied.",
            "Absolute references are essential when a formula needs to always point at one fixed cell (like a tax rate or exchange rate) while being copied down or across many rows/columns.",
        ],
        practice=[
            "Put a tax rate (like 0.18) in a fixed cell, and prices in a column. Write a formula multiplying a price by the tax rate using a relative reference, then copy it down and notice it breaks.",
            "Fix the formula by making the tax rate reference absolute ($) and copy it down again - confirm it now works correctly for every row.",
            "Select a reference inside a formula in the formula bar and press F4 repeatedly to see it cycle through reference styles.",
            "Create a mixed reference example: lock only the column of one reference, copy the formula across and down, and observe what moves and what doesn't.",
        ],
        quiz=[
            dict(q="What symbol makes a cell reference absolute?", options=["$", "#", "@", "!"], answer=0),
            dict(q="What happens to a relative reference when a formula is copied to a new cell?", options=["It shifts to match the new location", "It stays exactly the same", "It becomes an error", "It converts to text"], answer=0),
            dict(q="Which key cycles a selected reference through relative/absolute/mixed styles?", options=["F4", "F2", "F1", "F9"], answer=0),
            dict(q="Why would you use an absolute reference for a tax rate cell in a formula copied down many rows?", options=["So every row's formula always points to that one fixed cell", "So the formula runs faster", "So the tax rate cell gets bold formatting", "It is required for any formula with a rate"], answer=0),
        ],
    ),
    dict(
        id="U3C3",
        num=3,
        title="Basic Math Formulas & AutoSum",
        notes=[
            "SUM adds a list or range of numbers: =SUM(B2:B10).",
            "AVERAGE finds the mean of a range: =AVERAGE(B2:B10).",
            "MIN and MAX find the smallest and largest value in a range: =MIN(B2:B10), =MAX(B2:B10).",
            "COUNT counts how many cells in a range contain numbers; COUNTA counts how many cells are non-blank (numbers or text).",
            "AutoSum (the Sigma icon on the Home tab, or Alt+=) automatically guesses the range above/beside the active cell and inserts a SUM formula - it also has a dropdown for Average, Count, Min, Max.",
        ],
        practice=[
            "Enter a column of at least 6 numbers, click the cell below them, and press Alt+= to AutoSum.",
            "In another cell, write an AVERAGE formula for the same range.",
            "Write MIN and MAX formulas for the same range in two more cells.",
            "Add one text entry into the middle of a numeric range, then compare what COUNT vs COUNTA report for that range.",
        ],
        quiz=[
            dict(q="Which function adds up a range of numbers?", options=["SUM", "TOTAL", "ADD", "PLUS"], answer=0),
            dict(q="Which shortcut triggers AutoSum?", options=["Alt+=", "Ctrl+=", "Ctrl+S", "Alt+S"], answer=0),
            dict(q="What is the difference between COUNT and COUNTA?", options=["COUNT counts numeric cells only; COUNTA counts all non-blank cells", "They are identical", "COUNTA counts numeric cells only; COUNT counts all cells", "COUNT counts blank cells"], answer=0),
            dict(q="Which function finds the largest value in a range?", options=["MAX", "MIN", "SUM", "LARGE"], answer=0),
        ],
    ),
    dict(
        id="U3C4",
        num=4,
        title="Referencing Other Sheets & Workbooks",
        notes=[
            "To reference a cell on a different sheet, use SheetName!CellReference, e.g. =Sales!B2 pulls the value from cell B2 on the sheet named 'Sales'.",
            "If a sheet name has spaces, wrap it in single quotes: ='Monthly Sales'!B2.",
            "You can build formulas across sheets normally, e.g. =Sales!B2+Expenses!B2, adding a value from one sheet to a value from another.",
            "Referencing another workbook (a separate file) includes the file name in square brackets, e.g. =[Budget.xlsx]Sales!B2 - this only works reliably while both files are open, or the link may need updating.",
            "A Summary or Dashboard sheet commonly pulls numbers from several other sheets in the same workbook using this kind of cross-sheet reference.",
        ],
        practice=[
            "Create two sheets named 'Sales' and 'Summary'. Put a number in Sales!B2.",
            "On the Summary sheet, write a formula that references Sales!B2 directly.",
            "Change the value in Sales!B2 and confirm the Summary sheet updates automatically.",
            "Rename the Sales sheet to something with a space, like 'Sales Data', and notice how the formula reference changes to use quotes.",
        ],
        quiz=[
            dict(q="What is the correct way to reference cell B2 on a sheet named Sales?", options=["Sales!B2", "Sales.B2", "Sales->B2", "Sales:B2"], answer=0),
            dict(q="How do you reference a sheet whose name contains spaces, like 'Monthly Sales'?", options=["'Monthly Sales'!B2", "Monthly Sales!B2", "[Monthly Sales]B2", "Monthly_Sales!B2"], answer=0),
            dict(q="When referencing a cell in a different workbook, what is included in the reference?", options=["The file name in square brackets", "The file's save date", "Nothing extra is needed", "The file's folder path only"], answer=0),
            dict(q="What kind of sheet commonly pulls numbers from several other sheets via cross-sheet references?", options=["A Summary/Dashboard sheet", "Any blank sheet", "Only the first sheet in a workbook", "A hidden sheet only"], answer=0),
        ],
    ),
    dict(
        id="U3C5",
        num=5,
        title="Common Formula Errors & How to Read Them",
        notes=[
            "#DIV/0! means the formula is trying to divide by zero or by an empty cell.",
            "#VALUE! means the formula got the wrong type of value, e.g. trying to add a number to text.",
            "#REF! means a formula points to a cell reference that no longer exists, usually because that row/column/cell was deleted.",
            "#NAME? means Excel doesn't recognize something in the formula - often a misspelled function name or a missing quote around text.",
            "#N/A means a value wasn't found - most often seen with lookup functions (covered in Unit 7) when the search item doesn't exist in the data.",
            "Error checking (Formulas tab > Error Checking) and the small green triangle in a cell's corner can help flag and explain potential formula issues.",
        ],
        practice=[
            "Write a formula that divides a number by an empty cell and observe the #DIV/0! error.",
            "Write a formula that tries to add a number cell to a cell containing plain text and observe #VALUE!.",
            "Create a formula referencing a cell, delete that referenced cell's entire row, and observe the #REF! error appear.",
            "Deliberately misspell a function name (e.g. =SUME(B2:B5)) and observe the #NAME? error.",
        ],
        quiz=[
            dict(q="What does the #DIV/0! error mean?", options=["The formula is dividing by zero or a blank cell", "A reference was deleted", "A function name is misspelled", "A value wasn't found"], answer=0),
            dict(q="What does #REF! usually mean?", options=["A cell the formula pointed to no longer exists", "The formula divided by zero", "A function is misspelled", "The result is too large"], answer=0),
            dict(q="What commonly causes a #NAME? error?", options=["A misspelled function name", "Dividing by zero", "A deleted row", "A missing chart"], answer=0),
            dict(q="What does #VALUE! typically indicate?", options=["The formula received the wrong type of value, like text where a number was expected", "A file is missing", "A column is hidden", "A formula is too long"], answer=0),
        ],
    ),
    dict(
        id="U3C6",
        num=6,
        title="Formula Auditing: Trace Precedents, Dependents & Evaluate Formula",
        notes=[
            "Trace Precedents (Formulas tab) draws arrows showing which cells feed into the selected formula.",
            "Trace Dependents draws arrows showing which other formulas depend on the selected cell.",
            "These arrows are extremely useful for untangling a sheet you didn't build, or for finding what breaks if you delete a cell.",
            "Evaluate Formula (Formulas tab) steps through a complex formula one calculation at a time, showing intermediate results - great for debugging a formula that gives an unexpected answer.",
            "Remove Arrows (in the same menu) clears the trace arrows once you're done inspecting them.",
        ],
        practice=[
            "Build a small chain of 3 linked formulas (A feeds B, B feeds C).",
            "Select the final formula and use Trace Precedents to see arrows back to its sources.",
            "Select the first cell in the chain and use Trace Dependents to see what relies on it.",
            "Use Evaluate Formula on one of the formulas and step through it click by click.",
        ],
        quiz=[
            dict(q="What does Trace Precedents show?", options=["Which cells feed into the selected formula", "Which cells depend on the selected formula", "The formula's error type", "The formula's print area"], answer=0),
            dict(q="What does Trace Dependents show?", options=["Which other formulas rely on the selected cell", "Which cells feed into the formula", "The cell's format", "The cell's comments"], answer=0),
            dict(q="What does Evaluate Formula let you do?", options=["Step through a formula's calculation one stage at a time", "Automatically fix formula errors", "Convert a formula to a value", "Rename a formula"], answer=0),
            dict(q="What does Remove Arrows do?", options=["Clears the trace arrows from the sheet", "Deletes the formulas", "Deletes the cells", "Undoes the last edit"], answer=0),
        ],
    ),
    dict(
        id="U3C7",
        num=7,
        title="Naming Cells & Ranges (Introduction)",
        notes=[
            "A named range gives a plain-English name to a cell or range, e.g. naming cell B1 'TaxRate' instead of referring to it as B1 everywhere.",
            "To create one: select the cell/range, type a name directly into the Name Box (top-left), and press Enter.",
            "Once named, you can use that name inside formulas instead of the cell reference, e.g. =B2*TaxRate instead of =B2*$B$1 - often easier to read.",
            "Named ranges behave like absolute references automatically - they don't shift when a formula using them is copied elsewhere.",
            "Formulas > Name Manager lists every named range in the workbook, and lets you edit or delete them.",
        ],
        practice=[
            "Put a tax rate value in a cell, select it, type 'TaxRate' into the Name Box, and press Enter.",
            "Write a formula in another cell that multiplies a price by TaxRate using the name instead of the cell reference.",
            "Copy that formula down several rows and confirm TaxRate stays locked to the same cell automatically.",
            "Open Formulas > Name Manager and confirm your named range is listed there.",
        ],
        quiz=[
            dict(q="Where do you type a new name to create a named range?", options=["The Name Box", "The Formula Bar", "The Status Bar", "The Ribbon search box"], answer=0),
            dict(q="Do named ranges shift like relative references when a formula is copied?", options=["No, they stay locked like an absolute reference", "Yes, always", "Only if you press F4", "Only on the same sheet"], answer=0),
            dict(q="Where can you view and manage every named range in a workbook?", options=["Name Manager", "Format Cells", "Page Layout", "Data Validation"], answer=0),
            dict(q="What is one benefit of using =B2*TaxRate instead of =B2*$B$1?", options=["It's easier to read and understand at a glance", "It calculates faster", "It changes the cell's format", "It is required for AutoSum"], answer=0),
        ],
    ),
]

UNIT4_CHAPTERS = [
    dict(
        id="U4C1",
        num=1,
        title="IF Function Basics",
        notes=[
            "IF tests a condition and returns one value if it's true, and another if it's false: =IF(condition, value_if_true, value_if_false).",
            "Example: =IF(B2>=40, \"Pass\", \"Fail\") checks if a mark is 40 or above, and shows Pass or Fail accordingly.",
            "Comparison operators used in conditions: = (equal), <> (not equal), > (greater than), < (less than), >= (greater or equal), <= (less or equal).",
            "Text results in a formula must be wrapped in double quotes, like \"Pass\" - numbers don't need quotes.",
            "IF can return numbers, text, or even the result of another formula, not just fixed labels.",
        ],
        practice=[
            "Build a small marksheet with a Marks column. Write an IF formula that shows 'Pass' if marks are 40 or more, else 'Fail'.",
            "Copy the formula down for several students with different marks and confirm each one is correct.",
            "Change the pass threshold to 50 and see the results update.",
            "Write a second IF formula that gives a bonus of 10 if marks are above 90, else 0.",
        ],
        quiz=[
            dict(q="What is the correct structure of an IF formula?", options=["=IF(condition, value_if_true, value_if_false)", "=IF(value_if_true, condition, value_if_false)", "=IF(condition, value_if_false, value_if_true)", "=IF(condition)"], answer=0),
            dict(q="In =IF(B2>=40, \"Pass\", \"Fail\"), why is Pass in quotes?", options=["Text results must be wrapped in double quotes", "It is optional styling", "Quotes make it bold", "Quotes are required only for Fail"], answer=0),
            dict(q="Which operator means 'not equal to'?", options=["<>", "!=", "=/=", "><"], answer=0),
            dict(q="Can an IF formula's result be another formula's output instead of fixed text?", options=["Yes", "No, only fixed text is allowed", "No, only numbers are allowed", "Only in newer Excel versions"], answer=0),
        ],
    ),
    dict(
        id="U4C2",
        num=2,
        title="Nested IF & IFS",
        notes=[
            "A nested IF puts one IF formula inside another to test multiple conditions in sequence, e.g. grading A/B/C/F based on score ranges.",
            "Example: =IF(B2>=90,\"A\",IF(B2>=75,\"B\",IF(B2>=40,\"C\",\"F\"))) - Excel checks each condition in order until one is true.",
            "Nested IFs get hard to read past 2-3 levels deep - a common source of formula mistakes.",
            "IFS (newer versions of Excel) simplifies this: =IFS(B2>=90,\"A\",B2>=75,\"B\",B2>=40,\"C\",TRUE,\"F\") lists condition/result pairs without nesting.",
            "In IFS, a final TRUE condition acts as a catch-all 'else' - without one, IFS returns an #N/A error if no condition matches.",
        ],
        practice=[
            "Write a nested IF that assigns a grade (A/B/C/F) based on a score, with at least 3 thresholds.",
            "Test it against scores that fall in every grade band to confirm it works correctly.",
            "Rewrite the same logic using IFS instead, with a TRUE catch-all at the end.",
            "Remove the TRUE catch-all from your IFS formula and test a low score to see the #N/A result, then add it back.",
        ],
        quiz=[
            dict(q="What is a nested IF?", options=["An IF formula placed inside another IF formula", "An IF formula with no condition", "A formula that only works on text", "A type of chart"], answer=0),
            dict(q="What does a final TRUE condition do inside an IFS formula?", options=["Acts as a catch-all 'else' for anything not matched above", "Causes an error", "Repeats the first condition", "Is ignored by Excel"], answer=0),
            dict(q="What happens if no condition in an IFS formula matches and there's no TRUE catch-all?", options=["It returns an #N/A error", "It returns 0", "It returns blank with no error", "It returns the last value tried"], answer=0),
            dict(q="What is a common downside of deeply nested IF formulas?", options=["They become hard to read and error-prone", "They stop updating automatically", "They only work with numbers", "They cannot be copied down"], answer=0),
        ],
    ),
    dict(
        id="U4C3",
        num=3,
        title="AND, OR & NOT",
        notes=[
            "AND returns TRUE only if every condition inside it is true: =AND(B2>=40, C2>=40) checks two subjects both passed.",
            "OR returns TRUE if at least one condition inside it is true: =OR(B2=\"Yes\", C2=\"Yes\").",
            "NOT reverses a TRUE/FALSE result: =NOT(B2=\"\") is TRUE when B2 is not blank.",
            "AND/OR/NOT are most useful combined with IF, e.g. =IF(AND(B2>=40, C2>=40), \"Pass Both\", \"Check Again\").",
            "These functions can hold many conditions, not just two - separate each one with a comma.",
        ],
        practice=[
            "Build a sheet with two subject scores per student. Write an AND formula checking if both are 40+.",
            "Combine that AND formula inside an IF to display 'Pass Both' or 'Check Again'.",
            "Write an OR formula checking if either of two cells equals 'Yes'.",
            "Write a NOT formula that checks whether a cell is NOT blank.",
        ],
        quiz=[
            dict(q="When does AND return TRUE?", options=["Only when every condition inside it is true", "When at least one condition is true", "Always", "Never with text conditions"], answer=0),
            dict(q="When does OR return TRUE?", options=["When at least one condition inside it is true", "Only when every condition is true", "Only when all conditions are false", "Never"], answer=0),
            dict(q="What does NOT do to a TRUE/FALSE result?", options=["Reverses it", "Doubles it", "Ignores it", "Converts it to text"], answer=0),
            dict(q="Can AND/OR hold more than two conditions?", options=["Yes, separated by commas", "No, only exactly two", "Only in newer Excel versions", "Only OR can, not AND"], answer=0),
        ],
    ),
    dict(
        id="U4C4",
        num=4,
        title="Text Functions I: LEFT, RIGHT, MID & LEN",
        notes=[
            "LEFT pulls characters from the start of a text string: =LEFT(A2,3) grabs the first 3 characters.",
            "RIGHT pulls characters from the end: =RIGHT(A2,4) grabs the last 4 characters.",
            "MID pulls characters from the middle: =MID(A2,2,3) starts at position 2 and grabs the next 3 characters.",
            "LEN counts how many characters are in a text string, including spaces: =LEN(A2).",
            "These are commonly combined - e.g. LEN can tell MID how far to reach if the exact ending position isn't known in advance.",
        ],
        practice=[
            "Type a full name like 'Aditi Sharma' into a cell. Use LEFT to pull just the first 5 characters.",
            "Use RIGHT to pull the last 6 characters of the same cell.",
            "Use MID to pull characters 3 through 5.",
            "Use LEN to count the total characters in the cell, spaces included.",
        ],
        quiz=[
            dict(q="What does =LEFT(A2,3) do?", options=["Returns the first 3 characters of A2", "Returns the last 3 characters of A2", "Returns 3 characters starting from the middle", "Counts the characters in A2"], answer=0),
            dict(q="What does =RIGHT(A2,4) do?", options=["Returns the last 4 characters of A2", "Returns the first 4 characters of A2", "Deletes the last 4 characters", "Returns the 4th character only"], answer=0),
            dict(q="What does LEN count?", options=["The total number of characters in a text string, including spaces", "The number of words only", "The number of numbers only", "The row number"], answer=0),
            dict(q="What does =MID(A2,2,3) return?", options=["3 characters starting at position 2", "The 2nd character only", "Characters 2 through the end", "The first 2 characters plus 3 more"], answer=0),
        ],
    ),
    dict(
        id="U4C5",
        num=5,
        title="Text Functions II: Joining Text with & and TEXTJOIN",
        notes=[
            "The & operator joins (concatenates) text from different cells or literal text: =A2&\" \"&B2 joins a first and last name with a space between.",
            "CONCAT (newer) and the older CONCATENATE function both join multiple text pieces into one: =CONCAT(A2,\" \",B2).",
            "TEXTJOIN is more powerful: it joins a range with a chosen delimiter and can skip blanks, e.g. =TEXTJOIN(\", \",TRUE,A2:A5) joins A2 through A5 with commas, skipping any empty cells.",
            "Numbers joined with & become text automatically, e.g. =\"Total: \"&B2 turns the number in B2 into part of a text string.",
            "Joining text is commonly used to build full names, addresses, or readable labels from separate data columns.",
        ],
        practice=[
            "Put a first name in one cell and a last name in another. Use & to join them with a space in a third cell.",
            "Do the same join using TEXTJOIN with a comma-space delimiter instead.",
            "List 4 items in a column, some with blank cells among them, and use TEXTJOIN with TRUE to skip the blanks while joining them all with commas.",
            "Build a label like \"Total: 500\" by joining literal text with a number cell using &.",
        ],
        quiz=[
            dict(q="What does the & operator do in a formula?", options=["Joins text/values together", "Adds numbers only", "Divides values", "Compares two values"], answer=0),
            dict(q="What extra ability does TEXTJOIN have that plain & does not offer as conveniently?", options=["Joining a whole range with a delimiter, optionally skipping blanks", "Joining only two cells at a time", "Only working with numbers", "Automatically sorting values"], answer=0),
            dict(q="In =TEXTJOIN(\", \",TRUE,A2:A5), what does the TRUE argument do?", options=["Skips blank cells in the range", "Sorts the values alphabetically", "Rounds the numbers", "Converts text to uppercase"], answer=0),
            dict(q="What happens to a number when joined with & and text, like =\"Total: \"&B2?", options=["It becomes part of the resulting text string", "It causes a #VALUE! error", "It gets formatted as currency automatically", "It is ignored"], answer=0),
        ],
    ),
    dict(
        id="U4C6",
        num=6,
        title="Text Functions III: TRIM, UPPER/LOWER/PROPER, SUBSTITUTE & FIND",
        notes=[
            "TRIM removes extra spaces from text, keeping single spaces between words - very useful for cleaning data pasted from elsewhere.",
            "UPPER converts text to all capitals, LOWER converts to all lowercase, PROPER capitalizes the first letter of each word.",
            "SUBSTITUTE replaces specific text within a string: =SUBSTITUTE(A2,\"-\",\"/\") replaces every hyphen with a slash.",
            "FIND locates the position of text within a string and is case-sensitive; SEARCH does the same but ignores case - both return the starting position number, or an error if not found.",
            "These functions are commonly chained together to clean messy imported data, e.g. wrapping TRIM around PROPER around a raw name column.",
        ],
        practice=[
            "Type a name with extra spaces like '  Aditi   Sharma  ' into a cell, then use TRIM in another cell to clean it.",
            "Use UPPER, LOWER, and PROPER on the same text cell in three separate cells and compare results.",
            "Use SUBSTITUTE to replace all hyphens with slashes in a date-like text string, e.g. '31-01-2026'.",
            "Use FIND (or SEARCH) to locate the position of the '@' symbol in a sample email address text.",
        ],
        quiz=[
            dict(q="What does TRIM do?", options=["Removes extra spaces, keeping single spaces between words", "Deletes all spaces entirely", "Converts text to uppercase", "Counts characters"], answer=0),
            dict(q="Which function capitalizes the first letter of each word?", options=["PROPER", "UPPER", "LOWER", "TRIM"], answer=0),
            dict(q="What does SUBSTITUTE do?", options=["Replaces specific text within a string with other text", "Deletes a string entirely", "Counts occurrences of text", "Converts text to numbers"], answer=0),
            dict(q="What is the key difference between FIND and SEARCH?", options=["FIND is case-sensitive, SEARCH is not", "SEARCH is case-sensitive, FIND is not", "They are identical in every way", "FIND only works on numbers"], answer=0),
        ],
    ),
    dict(
        id="U4C7",
        num=7,
        title="Putting It Together: IF and Text Functions in Real Sheets",
        notes=[
            "Real-world sheets usually combine logical and text functions rather than using them in isolation - e.g. flagging rows based on both a number condition and a text pattern.",
            "Example: =IF(LEN(A2)=10,\"Valid Phone\",\"Check Number\") uses LEN inside IF to check formatting rules.",
            "Example: =IF(ISNUMBER(SEARCH(\"urgent\",A2)),\"Flag\",\"\") uses SEARCH with ISNUMBER inside IF to flag text containing a keyword, since SEARCH returns a number when found and an error when not.",
            "ISNUMBER, ISTEXT, and ISBLANK are simple checking functions that return TRUE/FALSE, often used inside IF to test what kind of data is in a cell before doing something with it.",
            "Building formulas like this in layers - test each piece separately, then combine - avoids frustrating troubleshooting once formulas get more complex.",
        ],
        practice=[
            "Build a small list of phone numbers, some with the wrong number of digits. Use LEN inside IF to flag entries that aren't exactly 10 characters.",
            "Build a small list of comments/notes. Use SEARCH with ISNUMBER inside IF to flag any row containing the word 'urgent'.",
            "Test ISNUMBER, ISTEXT, and ISBLANK directly on a few different cells (number, text, blank) to see how each responds.",
            "Combine two conditions with AND inside an IF to flag rows where a phone number is valid AND a status column says 'Active'.",
        ],
        quiz=[
            dict(q="Why does wrapping SEARCH inside ISNUMBER work well for flagging keyword matches inside IF?", options=["SEARCH returns a number when found and an error when not, and ISNUMBER converts that into TRUE/FALSE", "ISNUMBER makes SEARCH case-sensitive", "SEARCH cannot be used inside IF otherwise", "ISNUMBER sorts the results"], answer=0),
            dict(q="What does ISBLANK check?", options=["Whether a cell is empty", "Whether a cell contains a number", "Whether a cell contains text", "Whether a formula has an error"], answer=0),
            dict(q="What is a recommended approach when building a complex combined formula?", options=["Test each piece separately, then combine them", "Write the whole formula at once with no testing", "Avoid using more than one function ever", "Only test with blank cells"], answer=0),
            dict(q="In =IF(LEN(A2)=10,\"Valid Phone\",\"Check Number\"), what is being checked?", options=["Whether the text in A2 is exactly 10 characters long", "Whether A2 is a number", "Whether A2 is blank", "Whether A2 contains the word 'phone'"], answer=0),
        ],
    ),
]

UNIT5_CHAPTERS = [
    dict(
        id="U5C1",
        num=1,
        title="COUNTIF & COUNTIFS",
        notes=[
            "COUNTIF counts cells in a range that meet one condition: =COUNTIF(B2:B20,\"Pass\") counts how many cells equal 'Pass'.",
            "The condition (criteria) can use comparison operators as text, e.g. =COUNTIF(B2:B20,\">=40\") counts cells with a value of 40 or more.",
            "COUNTIFS extends this to multiple conditions across possibly different ranges, all of which must be true: =COUNTIFS(B2:B20,\">=40\",C2:C20,\"Active\").",
            "Wildcards work inside criteria: \"*\" matches any number of characters, \"?\" matches a single character, e.g. \"App*\" matches anything starting with App.",
            "COUNTIF/COUNTIFS are commonly used for quick summaries, like counting how many students passed, or how many orders are pending.",
        ],
        practice=[
            "Build a list of 10 students with Pass/Fail status. Use COUNTIF to count how many passed.",
            "Use COUNTIF with a >= condition to count how many students scored 60 or above in a marks column.",
            "Add a second column (like a class name) and use COUNTIFS to count students who passed AND are in a specific class.",
            "Use a wildcard criteria, like \"A*\", to count how many names in a list start with the letter A.",
        ],
        quiz=[
            dict(q="What does COUNTIF do?", options=["Counts cells in a range meeting one condition", "Sums cells meeting one condition", "Averages a range", "Counts every cell regardless of condition"], answer=0),
            dict(q="How many conditions can COUNTIFS check across ranges?", options=["Multiple conditions, all of which must be true", "Only exactly one", "Only two, never more", "Unlimited but only with OR logic"], answer=0),
            dict(q="What does the wildcard * match inside a COUNTIF criteria?", options=["Any number of characters", "Exactly one character", "Only numbers", "Only the start of text"], answer=0),
            dict(q="How would you write a COUNTIF condition to count values of 40 or more?", options=["\">=40\"", "\"=>40\"", "40>=", ">=40 (no quotes)"], answer=0),
        ],
    ),
    dict(
        id="U5C2",
        num=2,
        title="SUMIF & SUMIFS",
        notes=[
            "SUMIF adds up values in a range where a condition is met: =SUMIF(C2:C20,\"North\",D2:D20) sums column D wherever column C equals 'North'.",
            "The structure is =SUMIF(range_to_check, criteria, range_to_sum) - the range to check and the range to sum are often different columns.",
            "SUMIFS extends this to multiple conditions, all of which must be true: =SUMIFS(D2:D20,C2:C20,\"North\",E2:E20,\">100\").",
            "In SUMIFS, the sum range comes first, unlike SUMIF where the sum range comes last - a common source of mix-ups when switching between the two.",
            "Both functions accept the same comparison-operator and wildcard criteria style as COUNTIF/COUNTIFS.",
        ],
        practice=[
            "Build a small sales list with Region and Amount columns. Use SUMIF to total the Amount for a specific region.",
            "Add a Product column and use SUMIFS to total the Amount for a specific region AND a specific product.",
            "Use a >= condition inside SUMIFS to only total amounts above a certain value for a given region.",
            "Deliberately write the SUMIF arguments in the wrong order once to see the incorrect result, then fix it.",
        ],
        quiz=[
            dict(q="What is the argument order for SUMIF?", options=["range_to_check, criteria, range_to_sum", "range_to_sum, criteria, range_to_check", "criteria, range_to_check, range_to_sum", "range_to_check, range_to_sum, criteria"], answer=0),
            dict(q="What is the argument order for SUMIFS?", options=["sum_range, criteria_range1, criteria1, ...", "criteria1, criteria_range1, sum_range", "range_to_check, criteria, sum_range", "sum_range, criteria1, criteria_range1"], answer=0),
            dict(q="Can SUMIFS check more than one condition at once?", options=["Yes, and all conditions must be true", "No, only one condition ever", "Yes, but only with OR logic", "Only if both conditions are on the same column"], answer=0),
            dict(q="What does =SUMIF(C2:C20,\"North\",D2:D20) calculate?", options=["The sum of D2:D20 where the matching C cell equals 'North'", "The sum of C2:C20 only", "The count of cells equal to 'North'", "The average of D2:D20"], answer=0),
        ],
    ),
    dict(
        id="U5C3",
        num=3,
        title="AVERAGEIF & AVERAGEIFS",
        notes=[
            "AVERAGEIF averages values in a range where one condition is met: =AVERAGEIF(C2:C20,\"North\",D2:D20).",
            "AVERAGEIFS averages values where multiple conditions are all met, following the same 'average range first' pattern as SUMIFS.",
            "These follow the exact same argument logic as SUMIF/SUMIFS and COUNTIF/COUNTIFS - once you know one, the others are easy to pick up.",
            "If no cells match the condition(s), AVERAGEIF/AVERAGEIFS return a #DIV/0! error, since there's nothing to average.",
            "These are useful for questions like 'what's the average score for students in Class A' without manually filtering first.",
        ],
        practice=[
            "Using your sales/region sheet, use AVERAGEIF to find the average Amount for a specific region.",
            "Use AVERAGEIFS to find the average Amount for a specific region AND product combined.",
            "Deliberately use a condition that matches nothing and observe the #DIV/0! result.",
            "Compare the AVERAGEIF result to manually calculating SUM/COUNT for the same condition to confirm they match.",
        ],
        quiz=[
            dict(q="What does AVERAGEIF calculate?", options=["The average of values in a range where one condition is met", "The sum of values where a condition is met", "The count of matching cells", "The average of an entire column regardless of conditions"], answer=0),
            dict(q="What error appears if no cells match an AVERAGEIF condition?", options=["#DIV/0!", "#REF!", "#NAME?", "#VALUE!"], answer=0),
            dict(q="In AVERAGEIFS, which range is listed first?", options=["The average range", "The first criteria range", "The last criteria range", "It doesn't matter"], answer=0),
            dict(q="Why is it easy to learn AVERAGEIF/AVERAGEIFS after learning SUMIF/SUMIFS?", options=["They follow the same argument structure and logic", "They are the exact same function", "AVERAGEIF requires no arguments", "They only work on text"], answer=0),
        ],
    ),
    dict(
        id="U5C4",
        num=4,
        title="Data Validation: Dropdown Lists & Input Rules",
        notes=[
            "Data Validation (Data tab) restricts what can be typed into a cell, reducing typos and inconsistent entries.",
            "A dropdown list validation limits entry to a predefined list of options, e.g. only 'Pending', 'Active', 'Closed' for a status column.",
            "To create a dropdown: Data > Data Validation > Allow: List, then either type values separated by commas or point to a range containing the allowed values.",
            "Other validation types include Whole Number, Decimal, Date, and Text Length, each with min/max rules, e.g. only allowing marks between 0 and 100.",
            "The Input Message and Error Alert tabs (inside Data Validation) let you show a helpful hint before typing, and a custom warning if the entry breaks the rule.",
        ],
        practice=[
            "Create a Status column and set up a dropdown list validation with options like Pending, Active, Closed.",
            "Try typing something not on the list into that column and observe Excel block or warn about it.",
            "Add a Marks column with Whole Number validation restricted between 0 and 100.",
            "Add a custom Input Message that appears when a cell with validation is selected.",
        ],
        quiz=[
            dict(q="Where do you set up Data Validation?", options=["Data tab", "Home tab", "Insert tab", "Review tab"], answer=0),
            dict(q="What does a List validation type do?", options=["Restricts entries to a predefined set of options, often shown as a dropdown", "Restricts entries to numbers only", "Auto-fills the cell with a formula", "Highlights duplicate entries"], answer=0),
            dict(q="What can the Error Alert tab inside Data Validation do?", options=["Show a custom warning message when an invalid entry is typed", "Automatically fix the entry", "Delete the invalid entry", "Email the file owner"], answer=0),
            dict(q="What validation type would you use to only allow marks between 0 and 100?", options=["Whole Number with min/max rules", "List", "Text Length", "Date"], answer=0),
        ],
    ),
    dict(
        id="U5C5",
        num=5,
        title="Advanced Conditional Formatting: Formula-Based Rules",
        notes=[
            "Beyond the built-in rules from Unit 2, Conditional Formatting can use a custom formula: 'New Rule > Use a formula to determine which cells to format'.",
            "A formula-based rule must evaluate to TRUE or FALSE for each cell - when TRUE, the format is applied, e.g. =$C2=\"Overdue\" to highlight overdue rows.",
            "Formula-based rules can reference other cells in the same row to format an entire row based on one column's value, which the simple built-in rules can't do directly.",
            "Icon Sets add small icons (arrows, traffic lights, flags) based on value thresholds you control - useful for quick status-at-a-glance columns.",
            "Rule order matters: rules higher in Manage Rules are evaluated first, and you can check 'Stop If True' to prevent lower rules from also applying.",
        ],
        practice=[
            "Build a small task list with a Status column (Overdue, Pending, Done). Use a formula-based rule to highlight the entire row when Status = 'Overdue'.",
            "Apply an Icon Set to a numeric progress column to show traffic-light style status.",
            "Create two overlapping formula-based rules and use 'Stop If True' on one to control which wins.",
            "Open Manage Rules and reorder your rules to see how the display changes.",
        ],
        quiz=[
            dict(q="What must a formula-based conditional formatting rule evaluate to?", options=["TRUE or FALSE", "A number", "Text", "A cell reference"], answer=0),
            dict(q="What advantage does a formula-based rule have over simple built-in rules?", options=["It can format an entire row based on one column's value", "It works faster", "It doesn't need Manage Rules", "It can only be used with numbers"], answer=0),
            dict(q="What does 'Stop If True' do in Manage Rules?", options=["Prevents lower-priority rules from also applying once this one matches", "Deletes the rule", "Pauses all conditional formatting", "Converts the rule to a formula"], answer=0),
            dict(q="What do Icon Sets add to cells based on value thresholds?", options=["Small icons like arrows or traffic lights", "Borders", "Bold text", "Cell comments"], answer=0),
        ],
    ),
    dict(
        id="U5C6",
        num=6,
        title="Error Handling: IFERROR & IFNA",
        notes=[
            "IFERROR catches any error a formula produces and replaces it with something friendlier: =IFERROR(B2/C2,\"N/A\") shows 'N/A' instead of #DIV/0!.",
            "Structure: =IFERROR(original_formula, value_if_error) - if the original formula works fine, its normal result is shown; only on error does the fallback appear.",
            "IFNA is more specific - it only catches #N/A errors (common with lookups) and lets other error types (like #REF! or #DIV/0!) still show, which can be useful for not hiding real mistakes.",
            "Wrapping a formula in IFERROR is common practice in shared or presented sheets, so viewers see a clean message instead of a red error code.",
            "Be careful not to overuse IFERROR to hide genuine mistakes - it's best used where an error is an expected, normal outcome (like a lookup that legitimately might not find a match).",
        ],
        practice=[
            "Write a division formula that could divide by zero for some rows, then wrap it in IFERROR to show 'N/A' instead of the error.",
            "Compare a formula wrapped in IFERROR versus IFNA when the error type is #DIV/0! - notice IFNA lets it through.",
            "Deliberately create a #REF! error in a formula and wrap it with IFNA - confirm the real error still shows.",
            "Discuss (in your own notes) one situation where hiding an error with IFERROR could hide a genuine mistake instead of a normal outcome.",
        ],
        quiz=[
            dict(q="What does IFERROR do?", options=["Catches any formula error and replaces it with a fallback value", "Only catches #N/A errors", "Prevents formulas from ever erroring", "Deletes cells with errors"], answer=0),
            dict(q="How is IFNA different from IFERROR?", options=["IFNA only catches #N/A errors, letting other error types still show", "IFNA catches all errors like IFERROR", "IFNA only works with SUM", "IFNA cannot be combined with lookups"], answer=0),
            dict(q="What is a risk of overusing IFERROR?", options=["It can hide genuine mistakes instead of just expected outcomes", "It makes formulas calculate slower", "It disables AutoSum", "It converts formulas to text"], answer=0),
            dict(q="What is the second argument in =IFERROR(formula, fallback)?", options=["The value to show if the formula errors", "The original formula repeated", "A cell reference only", "A condition to test"], answer=0),
        ],
    ),
]

UNIT6_CHAPTERS = [
    dict(
        id="U6C1",
        num=1,
        title="Sorting Data (Single & Multi-Level)",
        notes=[
            "Sorting rearranges rows based on the values in one or more columns - it's found on the Data tab (Sort) or via the AutoFilter dropdown arrows.",
            "A single-level sort orders rows by just one column, A-Z or Z-A for text, smallest-largest or largest-smallest for numbers, oldest-newest for dates.",
            "A multi-level sort (Data > Sort, then 'Add Level') orders by one column first, then breaks ties using a second column, then a third, and so on.",
            "Always select your whole data range (or click any single cell inside a properly formatted table) before sorting, so related columns move together - sorting only one column in isolation scrambles a dataset.",
            "'My data has headers' should stay checked when sorting so the header row itself doesn't get treated as data and sorted into the middle.",
        ],
        practice=[
            "Build a small dataset with Name, Department, and Sales columns for at least 8 rows.",
            "Sort it by Sales, largest to smallest, using a single-level sort.",
            "Now do a multi-level sort: Department A-Z first, then Sales largest-to-smallest within each department.",
            "Select only the Sales column (not the whole row) and sort it in isolation - observe how the data gets scrambled, then undo.",
        ],
        quiz=[
            dict(q="What does a multi-level sort let you do?", options=["Sort by one column, then break ties using another column", "Sort only numeric columns", "Sort without selecting data first", "Sort columns instead of rows"], answer=0),
            dict(q="Why is it risky to sort just one column in isolation within a multi-column dataset?", options=["It scrambles the relationship between that row's other values", "It deletes the column", "It converts numbers to text", "It is not actually possible in Excel"], answer=0),
            dict(q="What should stay checked when sorting a range that includes column headers?", options=["'My data has headers'", "'Case sensitive'", "'Sort left to right'", "'Ignore blanks'"], answer=0),
            dict(q="Where do you add a second sort level?", options=["In the Sort dialog, using 'Add Level'", "In Format Cells", "In Data Validation", "In Conditional Formatting"], answer=0),
        ],
    ),
    dict(
        id="U6C2",
        num=2,
        title="Filtering Data (AutoFilter & Custom Filters)",
        notes=[
            "AutoFilter (Data tab > Filter, or Ctrl+Shift+L) adds dropdown arrows to each column header, letting you show only rows matching chosen criteria.",
            "Filtering hides non-matching rows - it doesn't delete them; turning the filter off (or choosing 'Clear') brings every row back into view.",
            "Text filters offer options like 'Contains', 'Begins With', 'Ends With'; number filters offer 'Greater Than', 'Between', 'Top 10', etc.",
            "Multiple column filters combine together (AND logic) - e.g. filtering Department = 'Sales' and Status = 'Active' shows only rows matching both.",
            "The Status Bar still shows live counts/sums for visible (filtered) cells only, which is useful for quick checks on a subset of data.",
        ],
        practice=[
            "Turn on AutoFilter for your dataset from the previous chapter.",
            "Filter the Department column to show only one department.",
            "Add a second filter on the Sales column to show only sales above a certain number, combined with the department filter.",
            "Clear all filters and confirm every row reappears.",
        ],
        quiz=[
            dict(q="Does AutoFilter delete the rows that don't match the criteria?", options=["No, it just hides them from view", "Yes, permanently", "Yes, but Undo can bring them back only once", "It moves them to a new sheet"], answer=0),
            dict(q="What shortcut toggles AutoFilter on/off?", options=["Ctrl+Shift+L", "Ctrl+F", "Ctrl+Shift+F", "Alt+F"], answer=0),
            dict(q="When two columns each have a filter applied, how do they combine?", options=["AND logic - rows must match both filters", "OR logic - rows must match either filter", "Only the last filter applied matters", "They cancel each other out"], answer=0),
            dict(q="What does the Status Bar show when a filter is active?", options=["Live counts/sums for the visible (filtered) cells only", "Totals for the entire dataset regardless of filter", "Nothing changes", "Only the filter criteria used"], answer=0),
        ],
    ),
    dict(
        id="U6C3",
        num=3,
        title="Removing Duplicates",
        notes=[
            "Data > Remove Duplicates scans selected columns and deletes rows that repeat, keeping only the first occurrence.",
            "You choose which columns must match for a row to count as a duplicate - e.g. matching only on Email even if Name differs slightly.",
            "Remove Duplicates permanently deletes rows - always keep a backup copy of the data, or work on a copy of the sheet, before running it.",
            "Conditional Formatting > Highlight Cells Rules > Duplicate Values can flag duplicates visually first, letting you review before deciding to delete anything.",
            "COUNTIF can also detect duplicates without deleting anything, e.g. =COUNTIF(A:A,A2)>1 flags a value that appears more than once.",
        ],
        practice=[
            "Build a list of 10 rows with a few deliberately duplicated entries (same value across all columns for at least 2 rows).",
            "Use Conditional Formatting > Duplicate Values to visually highlight the duplicates first.",
            "Make a copy of the sheet, then use Remove Duplicates on the copy and compare row counts before/after.",
            "Write a COUNTIF formula that flags any value appearing more than once in a single column.",
        ],
        quiz=[
            dict(q="When Remove Duplicates finds repeated rows, which one does it keep?", options=["The first occurrence", "The last occurrence", "A random one", "None, it deletes all copies"], answer=0),
            dict(q="Why is it recommended to back up data before using Remove Duplicates?", options=["It permanently deletes rows", "It only works on a copy automatically", "It can corrupt the file", "It cannot be undone with Ctrl+Z"], answer=0),
            dict(q="How can you flag duplicates without deleting anything?", options=["Conditional Formatting's Duplicate Values rule, or a COUNTIF formula", "There is no way to do this", "Only by manually checking every row", "By using Freeze Panes"], answer=0),
            dict(q="What does =COUNTIF(A:A,A2)>1 check?", options=["Whether the value in A2 appears more than once in column A", "Whether A2 is blank", "Whether A2 is the largest value", "Whether A2 is a duplicate of A1 only"], answer=0),
        ],
    ),
    dict(
        id="U6C4",
        num=4,
        title="Text to Columns",
        notes=[
            "Text to Columns (Data tab) splits a single column's content into multiple columns based on a delimiter (like a comma, space, or hyphen) or fixed width.",
            "Common use case: a 'Full Name' column pasted from elsewhere needs to be split into separate 'First Name' and 'Last Name' columns.",
            "Delimited mode splits wherever a chosen character appears (comma, tab, space, semicolon, or a custom character); Fixed Width mode splits at specific character positions you click to set.",
            "Text to Columns can also be used just to fix data type - e.g. running it on a single selected column with 'General' format can convert text-that-looks-like-numbers into real numbers.",
            "Always leave enough empty columns to the right before running Text to Columns, or it will overwrite existing data in those columns.",
        ],
        practice=[
            "Type 3-4 full names like 'Aditi Sharma' into a single column.",
            "Select that column and use Text to Columns with Space as the delimiter to split into First Name and Last Name.",
            "Build a column of comma-separated values like 'Mumbai,Delhi,Pune' and split it into 3 columns using comma as the delimiter.",
            "Try Fixed Width mode on a column of consistently-formatted codes (like 3-letter + 4-digit codes) to split at a specific position.",
        ],
        quiz=[
            dict(q="What does Text to Columns do?", options=["Splits one column's content into multiple columns", "Merges multiple columns into one", "Deletes empty columns", "Sorts a column alphabetically"], answer=0),
            dict(q="What are the two main splitting modes in Text to Columns?", options=["Delimited and Fixed Width", "Ascending and Descending", "Text and Number", "Manual and Automatic"], answer=0),
            dict(q="Why should you leave empty columns to the right before running Text to Columns?", options=["It can overwrite existing data in those columns", "It requires extra columns to function at all", "It automatically inserts new rows", "It changes the row height"], answer=0),
            dict(q="What is one common use case for Text to Columns?", options=["Splitting a Full Name column into First Name and Last Name", "Adding borders to a table", "Creating a chart", "Freezing panes"], answer=0),
        ],
    ),
    dict(
        id="U6C5",
        num=5,
        title="Flash Fill",
        notes=[
            "Flash Fill (Data tab, or Ctrl+E) detects a pattern from an example you type and automatically fills the rest of the column to match.",
            "Example: if column A has 'Aditi Sharma' and you type 'Aditi' in column B, Flash Fill can detect you're extracting first names and fill the rest of column B automatically.",
            "Flash Fill works on patterns like extracting parts of text, combining columns, or reformatting text (e.g. changing date styles) - no formula required.",
            "It only takes effect after you've given at least one (often two) clear example(s) and either accept its suggestion or press Ctrl+E.",
            "Flash Fill produces static values, not a live formula - if the source data changes later, Flash Fill results won't update automatically and must be re-run.",
        ],
        practice=[
            "Put 4 full names in column A. In column B, type just the first name of row 1's example, then press Ctrl+E and watch the rest fill in.",
            "In column C, try combining first and last name differently (e.g. 'Sharma, Aditi') by typing one example and using Flash Fill.",
            "Change one of the original names in column A afterward and confirm the Flash Fill result in column B does NOT update automatically.",
            "Compare this to a TEXTJOIN or & formula approach from Unit 4, which would update automatically.",
        ],
        quiz=[
            dict(q="What does Flash Fill do?", options=["Detects a pattern from your example and fills the rest of the column to match", "Colors cells based on value", "Sorts a column automatically", "Removes duplicate rows"], answer=0),
            dict(q="Which shortcut triggers Flash Fill?", options=["Ctrl+E", "Ctrl+F", "Ctrl+D", "Ctrl+Shift+E"], answer=0),
            dict(q="Does a Flash Fill result update automatically if the source data changes later?", options=["No, it produces static values", "Yes, just like a formula", "Only if Ctrl+E is held down", "Only for numbers, not text"], answer=0),
            dict(q="What kind of examples does Flash Fill need to detect a pattern?", options=["At least one clear example typed in the target column", "A full formula written out first", "A named range", "A chart showing the pattern"], answer=0),
        ],
    ),
    dict(
        id="U6C6",
        num=6,
        title="Find & Replace (Including Wildcards)",
        notes=[
            "Find (Ctrl+F) searches a sheet or workbook for matching text/values; Replace (Ctrl+H) searches and swaps matches for something else.",
            "Options include matching case, matching the entire cell content only, and searching within the current sheet vs the whole workbook.",
            "Wildcards work in Find & Replace too: * matches any number of characters, ? matches exactly one character - useful for pattern-based cleanup.",
            "'Replace All' changes every match at once; 'Replace' lets you step through matches one at a time and confirm each individually - safer for uncertain cleanup.",
            "Find & Replace can also search within formulas, values, or comments specifically, using the 'Within' and 'Look in' options in the dialog.",
        ],
        practice=[
            "Use Ctrl+F to find a specific value somewhere in your practice workbook.",
            "Use Ctrl+H to replace every occurrence of one word/value with another across a sheet.",
            "Use a wildcard search like 'Sh*' to find every entry starting with 'Sh' in a name column.",
            "Try 'Replace' (not 'Replace All') to step through matches one at a time and confirm/skip each.",
        ],
        quiz=[
            dict(q="Which shortcut opens Find and Replace directly?", options=["Ctrl+H", "Ctrl+F", "Ctrl+R", "Ctrl+E"], answer=0),
            dict(q="What does the wildcard ? match in Find & Replace?", options=["Exactly one character", "Any number of characters", "Only numbers", "Only the first letter"], answer=0),
            dict(q="What is the difference between 'Replace' and 'Replace All'?", options=["'Replace' steps through matches one at a time; 'Replace All' changes every match instantly", "They are identical", "'Replace All' only works on formulas", "'Replace' only works on the active cell"], answer=0),
            dict(q="Where can you choose whether Find searches the current sheet or the whole workbook?", options=["The 'Within' option in the Find/Replace dialog", "The Name Box", "The Status Bar", "Conditional Formatting"], answer=0),
        ],
    ),
    dict(
        id="U6C7",
        num=7,
        title="Data Cleaning Workflow: Putting It Together",
        notes=[
            "A typical cleaning pass on messy imported data: trim extra spaces, fix inconsistent capitalization, remove duplicates, split combined fields, and check for blanks - roughly in that order.",
            "Always work on a copy of raw data (a duplicated sheet or a backup file) before cleaning - once destructive steps like Remove Duplicates run, the original mess is hard to recover.",
            "A quick blank-check: select a column and look at the Status Bar's Count vs a COUNTA formula - a mismatch can reveal unexpected blanks.",
            "Combining what you've learned: TRIM+PROPER can fix names, Text to Columns or Flash Fill can split combined fields, Remove Duplicates removes repeats, and Data Validation prevents new mess going forward.",
            "Cleaning is rarely 'one function fixes everything' - it's usually several small, verifiable steps applied in sequence, checking results after each one.",
        ],
        practice=[
            "Build a deliberately messy dataset: inconsistent capitalization, extra spaces, a combined name column, and a couple of exact duplicate rows.",
            "Make a backup copy of the sheet before touching it.",
            "Clean it step by step: TRIM+PROPER the names, split the combined column, then remove duplicates.",
            "Compare row counts and a few sample values between the messy original and the cleaned copy to confirm nothing was lost incorrectly.",
        ],
        quiz=[
            dict(q="Why should you work on a copy of raw data before cleaning it?", options=["Destructive steps like Remove Duplicates are hard to reverse once run", "Excel requires a backup file to open", "Cleaning always corrupts the original file automatically", "It is required for Conditional Formatting to work"], answer=0),
            dict(q="What is a reasonable general order for a cleaning pass?", options=["Trim spaces, fix capitalization, remove duplicates, split fields, check blanks", "Remove duplicates first, then never check anything else", "Only run one function and stop", "Split fields before entering any data"], answer=0),
            dict(q="How can you quickly check for unexpected blanks in a column?", options=["Compare Status Bar Count to a COUNTA formula result", "Apply Currency format", "Use Freeze Panes", "Use Merge & Center"], answer=0),
            dict(q="Is data cleaning usually a single function, or several small steps?", options=["Several small, verifiable steps applied in sequence", "A single function that fixes everything at once", "Something Excel does automatically on open", "Only possible with VBA"], answer=0),
        ],
    ),
]

UNIT7_CHAPTERS = [
    dict(
        id="U7C1",
        num=1,
        title="VLOOKUP Basics",
        notes=[
            "VLOOKUP searches for a value in the leftmost column of a range and returns a value from a chosen column in the same row: =VLOOKUP(lookup_value, table_range, column_number, [range_lookup]).",
            "Example: =VLOOKUP(A2, Products!A:C, 3, FALSE) looks up A2 in the Products sheet's first column, then returns the value from the 3rd column of that range, in the matching row.",
            "The 4th argument, range_lookup, should almost always be FALSE (exact match) for lookups like product codes or IDs - TRUE (approximate match) requires sorted data and is easy to misuse.",
            "VLOOKUP can only look to the right of its search column - it cannot search a column and return a value from a column to its left.",
            "The column_number argument counts columns within the selected table_range, starting at 1 for the leftmost column of that range - not the sheet's absolute column letter.",
        ],
        practice=[
            "Build a small Products sheet with columns: Product Code, Product Name, Price (at least 6 rows).",
            "On a new sheet, list a few product codes and use VLOOKUP with FALSE to pull each Product Name.",
            "Add a second VLOOKUP to pull the Price for the same codes.",
            "Deliberately use TRUE instead of FALSE once on unsorted data and observe an incorrect result.",
        ],
        quiz=[
            dict(q="What does the 4th argument of VLOOKUP control?", options=["Whether it's an exact (FALSE) or approximate (TRUE) match", "Which column to search in", "Which sheet to search", "Whether the result is bold"], answer=0),
            dict(q="Can VLOOKUP look to the left of its search column?", options=["No, only to the right", "Yes, always", "Only with TRUE match", "Only in newer Excel versions"], answer=0),
            dict(q="In =VLOOKUP(A2, Products!A:C, 3, FALSE), what does the 3 refer to?", options=["The 3rd column within the selected range, not the sheet's column letter", "The 3rd row of the range", "The 3rd sheet in the workbook", "The 3rd match found"], answer=0),
            dict(q="Why should range_lookup usually be FALSE for something like a product code lookup?", options=["It requires an exact match rather than an approximate one on sorted data", "FALSE is required by Excel for all lookups", "TRUE only works with text", "FALSE makes the formula calculate faster"], answer=0),
        ],
    ),
    dict(
        id="U7C2",
        num=2,
        title="VLOOKUP Pitfalls & Troubleshooting",
        notes=[
            "A #N/A error from VLOOKUP usually means the lookup value truly isn't in the search column - check for extra spaces, mismatched text/number types, or typos as common hidden causes.",
            "If a lookup value is a number stored as text (or vice versa) in one of the two places being compared, VLOOKUP will fail to match even though the values look identical on screen.",
            "VLOOKUP's table_range should usually be locked with absolute references ($) when copied down, so every row searches the same full table instead of a shifting one.",
            "VLOOKUP returns the FIRST matching row it finds - if the search column has duplicate values, later matches are silently ignored.",
            "Wrapping VLOOKUP in IFERROR (from Unit 5) is common practice to show a clean message instead of #N/A when a lookup legitimately might not find a match.",
        ],
        practice=[
            "Deliberately add extra trailing spaces to one lookup value's source cell and observe VLOOKUP fail to find a match that looks identical visually.",
            "Fix it using TRIM on the lookup value, or on the source data, and confirm the match now works.",
            "Copy a working VLOOKUP formula down several rows without locking the table range with $, and observe it break partway down; fix it with absolute references.",
            "Wrap a VLOOKUP formula in IFERROR to show 'Not Found' instead of #N/A for a deliberately missing code.",
        ],
        quiz=[
            dict(q="What is a common hidden cause of VLOOKUP #N/A errors even when values look identical?", options=["Extra spaces or mismatched text/number types", "Using too many rows", "The sheet being too wide", "Using a chart on the same sheet"], answer=0),
            dict(q="Why lock the table_range with $ when copying a VLOOKUP formula down?", options=["So every row searches the same full table instead of a shifting range", "It is required syntax for VLOOKUP to work at all", "It makes the lookup case-sensitive", "It prevents #N/A errors entirely"], answer=0),
            dict(q="If the search column has duplicate values, which match does VLOOKUP return?", options=["The first matching row it finds", "The last matching row", "All matching rows at once", "An error, always"], answer=0),
            dict(q="What is a common way to handle an expected, normal 'not found' case in VLOOKUP?", options=["Wrap it in IFERROR to show a clean message", "Delete the formula", "Ignore the #N/A, it fixes itself", "Switch to TRUE match"], answer=0),
        ],
    ),
    dict(
        id="U7C3",
        num=3,
        title="HLOOKUP",
        notes=[
            "HLOOKUP works like VLOOKUP but searches horizontally: it looks across the top row of a range and returns a value from a chosen row number below it.",
            "Structure: =HLOOKUP(lookup_value, table_range, row_number, [range_lookup]) - the arguments mirror VLOOKUP, just rotated 90 degrees.",
            "HLOOKUP is used far less often than VLOOKUP in practice, because most real-world data tables are organized with fields as columns (vertical), not rows (horizontal).",
            "Just like VLOOKUP, HLOOKUP's row_number counts from the top of the selected table_range starting at 1, and range_lookup should usually be FALSE for exact matches.",
            "A dataset laid out with categories across the top row (e.g. Jan, Feb, Mar as column headers) and one value per month below is a typical case where HLOOKUP fits naturally.",
        ],
        practice=[
            "Build a small table with months (Jan, Feb, Mar, Apr) across the top row, and a single row of sales figures below.",
            "Use HLOOKUP to pull the sales figure for a specific month by name.",
            "Add a second data row (e.g. Expenses) below the Sales row and adjust the row_number to pull from that row instead.",
            "Compare how this table layout differs from the Product table you used for VLOOKUP earlier - note why VLOOKUP wouldn't fit this shape well.",
        ],
        quiz=[
            dict(q="What does HLOOKUP search, compared to VLOOKUP?", options=["The top row of a range, searching horizontally", "The leftmost column, same as VLOOKUP", "A diagonal path across the sheet", "Only the last row"], answer=0),
            dict(q="In HLOOKUP, what does the row_number argument refer to?", options=["Which row within the table_range to return a value from, counting from the top", "Which column to search", "The sheet number", "The number of matches to return"], answer=0),
            dict(q="Why is HLOOKUP used less often than VLOOKUP in practice?", options=["Most real-world tables organize fields as columns, not rows", "HLOOKUP doesn't support exact match", "HLOOKUP only works with numbers", "HLOOKUP was removed from modern Excel"], answer=0),
            dict(q="What table layout naturally fits HLOOKUP?", options=["Categories across the top row, like months as column headers", "A single column list", "A pivot table only", "A chart"], answer=0),
        ],
    ),
    dict(
        id="U7C4",
        num=4,
        title="INDEX & MATCH",
        notes=[
            "MATCH finds the position of a value within a range: =MATCH(lookup_value, range, 0) returns a position number (0 means exact match), not the value itself.",
            "INDEX returns the value at a given position within a range: =INDEX(range, row_number) - it needs a position number to know what to return.",
            "Combined, INDEX+MATCH replicates and improves on VLOOKUP: =INDEX(return_range, MATCH(lookup_value, search_range, 0)) - MATCH finds the row position, INDEX fetches the result from any column.",
            "Unlike VLOOKUP, INDEX+MATCH can look up values to the LEFT of the search column, since the return range and search range are specified independently.",
            "INDEX+MATCH is also less fragile when columns are inserted or reordered in the table, since it doesn't rely on counting a fixed column number like VLOOKUP does.",
        ],
        practice=[
            "Using your Products sheet, write a MATCH formula that finds the position of a specific product code in the code column.",
            "Write a separate INDEX formula that returns the value at that position from the Product Name column.",
            "Combine them into one INDEX+MATCH formula that looks up a product code and returns the Product Name directly.",
            "Rearrange your Products sheet so Product Name is now to the LEFT of Product Code, and confirm INDEX+MATCH still works where VLOOKUP would have failed.",
        ],
        quiz=[
            dict(q="What does MATCH return?", options=["The position of a value within a range", "The value itself", "TRUE or FALSE", "A formatted result"], answer=0),
            dict(q="What does INDEX need to return a value?", options=["A position number within a range", "A column letter", "A sheet name", "A cell color"], answer=0),
            dict(q="What can INDEX+MATCH do that plain VLOOKUP cannot?", options=["Look up values to the left of the search column", "Search multiple sheets at once", "Work without any arguments", "Return more than one result at a time automatically"], answer=0),
            dict(q="Why is INDEX+MATCH considered less fragile than VLOOKUP when columns are inserted?", options=["It doesn't rely on counting a fixed column number", "It automatically updates the table range", "It ignores inserted columns entirely", "It uses absolute references only"], answer=0),
        ],
    ),
    dict(
        id="U7C5",
        num=5,
        title="XLOOKUP (The Modern Replacement)",
        notes=[
            "XLOOKUP (available in newer Excel versions) is a single, simpler function that replaces most VLOOKUP, HLOOKUP, and even many INDEX+MATCH use cases.",
            "Structure: =XLOOKUP(lookup_value, lookup_array, return_array, [if_not_found]) - the search range and return range are specified separately, so it can look in any direction.",
            "XLOOKUP defaults to an exact match, unlike VLOOKUP which defaults to approximate match unless you remember to set FALSE - a common source of past VLOOKUP mistakes is avoided.",
            "The optional if_not_found argument replaces the need to wrap the formula in IFERROR: =XLOOKUP(A2, CodeRange, NameRange, \"Not Found\").",
            "If your version of Excel has XLOOKUP available, it's generally the best default choice for new lookup formulas going forward, though VLOOKUP and INDEX+MATCH remain common in older shared workbooks.",
        ],
        practice=[
            "If your Excel version has XLOOKUP, rewrite one of your earlier VLOOKUP formulas as an XLOOKUP instead.",
            "Use the if_not_found argument directly in an XLOOKUP instead of wrapping it in IFERROR.",
            "Use XLOOKUP to look up a value to the LEFT of the search column, the same way you did with INDEX+MATCH.",
            "If XLOOKUP isn't available in your version, read through this chapter's notes and compare them conceptually to your INDEX+MATCH formula from the previous chapter.",
        ],
        quiz=[
            dict(q="What does XLOOKUP default to for matching, unlike VLOOKUP?", options=["Exact match", "Approximate match", "Wildcard match", "Case-sensitive match only"], answer=0),
            dict(q="What does the if_not_found argument in XLOOKUP replace the need for?", options=["Wrapping the formula in IFERROR", "Using absolute references", "Sorting the data first", "Using Text to Columns"], answer=0),
            dict(q="Can XLOOKUP look up values to the left of its search column, like INDEX+MATCH?", options=["Yes", "No, only to the right like VLOOKUP", "Only with TRUE match", "Only with HLOOKUP combined"], answer=0),
            dict(q="Why might VLOOKUP and INDEX+MATCH still be common even where XLOOKUP is available?", options=["Older shared workbooks may have been built before XLOOKUP existed", "XLOOKUP cannot handle text values", "XLOOKUP is slower", "VLOOKUP is required for AutoSum"], answer=0),
        ],
    ),
    dict(
        id="U7C6",
        num=6,
        title="Nested Lookups & Combining with IF",
        notes=[
            "Lookup functions are often combined with IF to react differently depending on what was found, e.g. showing a discount message only for certain looked-up categories.",
            "A lookup can also feed directly into another function's argument, e.g. using a looked-up price inside a SUM or multiplication formula rather than displaying it alone.",
            "Two-way lookups combine MATCH twice (once for row position, once for column position) inside INDEX, useful for grid-shaped tables like a price list by size AND color.",
            "When nesting lookups, build and test each piece separately first (as suggested in Unit 4), then combine - nested lookup formulas are hard to debug all at once.",
            "A lookup result can also be used as the lookup_value for a second, different lookup - e.g. looking up a customer's region, then using that region to look up a shipping rate.",
        ],
        practice=[
            "Build a small two-way table: sizes (S, M, L) across the top, colors down the side, and prices in the grid.",
            "Use INDEX with two MATCH functions (row and column) to look up a price for a specific size AND color combination.",
            "Build a chained lookup: look up a customer's region from one table, then use that region to look up a shipping rate from a second table.",
            "Wrap one of your nested lookups in IF to show a custom message when the combination isn't found.",
        ],
        quiz=[
            dict(q="How does a two-way lookup typically work using INDEX?", options=["INDEX with two MATCH functions, one for row position and one for column position", "Two separate VLOOKUP formulas averaged together", "HLOOKUP run twice", "A single MATCH with two lookup values"], answer=0),
            dict(q="What is recommended before combining multiple lookups into one nested formula?", options=["Build and test each piece separately first", "Write the whole nested formula at once with no testing", "Avoid ever nesting lookups", "Use only HLOOKUP for nested cases"], answer=0),
            dict(q="What does a 'chained lookup' mean in this context?", options=["A lookup result is used as the lookup_value for a second, different lookup", "Looking up the same value twice for confirmation", "Combining SUM and AVERAGE", "Using VLOOKUP and HLOOKUP on the same cell simultaneously"], answer=0),
            dict(q="Why is a grid-shaped table (sizes across top, colors down side) a good case for a two-way lookup?", options=["It needs both a row and a column position to find one specific value", "It only has one dimension of data", "It cannot use INDEX at all", "It requires HLOOKUP exclusively"], answer=0),
        ],
    ),
    dict(
        id="U7C7",
        num=7,
        title="CHOOSE, OFFSET & INDIRECT (Introduction)",
        notes=[
            "CHOOSE picks a value from a list based on a position number: =CHOOSE(2,\"Low\",\"Medium\",\"High\") returns 'Medium' since it's the 2nd item listed.",
            "OFFSET returns a reference to a range that is a certain number of rows/columns away from a starting point: =OFFSET(A1,2,1) refers to the cell 2 rows down and 1 column right of A1.",
            "INDIRECT turns a text string into an actual cell reference: =INDIRECT(\"A1\") behaves as if you'd typed =A1, but the text can be built dynamically from other cells.",
            "OFFSET and INDIRECT are powerful but 'volatile' functions - they recalculate on every single sheet change, which can slow down very large workbooks if overused.",
            "These are introduced here at a basic level; they're used more heavily in advanced dynamic dashboards, which is beyond this course's current scope - simpler alternatives (like the dynamic array functions in Unit 8) are often preferable for everyday use.",
        ],
        practice=[
            "Write a CHOOSE formula that picks one of 4 text labels based on a number you can change in another cell.",
            "Write an OFFSET formula that returns a value a few rows below and a column to the right of a starting cell, then change the starting cell and watch it shift.",
            "Write an INDIRECT formula using a hard-coded cell reference text string, like =INDIRECT(\"B2\").",
            "Build a cell containing the text 'B2' and use INDIRECT on that cell instead, to see how the reference becomes dynamic.",
        ],
        quiz=[
            dict(q="What does CHOOSE do?", options=["Picks a value from a list based on a position number", "Looks up a value in a table", "Counts matching cells", "Sorts a list"], answer=0),
            dict(q="What does OFFSET return?", options=["A reference to a range a certain number of rows/columns from a starting point", "The sum of a range", "A formatted date", "A random value"], answer=0),
            dict(q="What does INDIRECT do?", options=["Turns a text string into an actual cell reference", "Converts a reference into text", "Hides a cell", "Looks up a value like VLOOKUP"], answer=0),
            dict(q="Why should OFFSET and INDIRECT be used carefully in large workbooks?", options=["They are 'volatile' and recalculate on every sheet change, which can slow things down", "They are not supported in any Excel version", "They only work with text", "They automatically delete data"], answer=0),
        ],
    ),
]

UNIT8_CHAPTERS = [
    dict(
        id="U8C1",
        num=1,
        title="Excel Tables (Ctrl+T) & Structured References",
        notes=[
            "Selecting your data and pressing Ctrl+T converts a plain range into an Excel Table - a smarter, self-maintaining data structure.",
            "Tables automatically extend formulas, formatting, and filters to new rows added at the bottom - no more manually dragging formulas down after adding data.",
            "Tables come with structured references: instead of =B2*C2, a table formula can read =[@Price]*[@Qty], which is easier to understand and doesn't break when rows are inserted.",
            "Each table gets a name (default Table1, Table2...) that can be changed in Table Design tab - useful for referring to the whole table in formulas elsewhere, like =SUM(Table1[Price]).",
            "Tables also get built-in filter dropdowns automatically and a Total Row option (Table Design tab) for quick column summaries.",
        ],
        practice=[
            "Select a dataset with headers and press Ctrl+T to convert it into a Table.",
            "Add a new row at the bottom of the table and type data - confirm formulas/formatting from other columns extend automatically.",
            "Add a calculated column using a structured reference formula, like [@Price]*[@Qty].",
            "Turn on the Total Row and try a couple of different summary functions (Sum, Average) on a numeric column.",
        ],
        quiz=[
            dict(q="What shortcut converts a range into an Excel Table?", options=["Ctrl+T", "Ctrl+R", "Ctrl+Shift+T", "Alt+T"], answer=0),
            dict(q="What happens automatically when you add a new row to the bottom of a Table?", options=["Formulas and formatting extend to it automatically", "Nothing changes", "The table splits into two", "It must be manually reformatted"], answer=0),
            dict(q="What is a structured reference like [@Price]?", options=["A way to reference a table column that's easier to read and doesn't break when rows are inserted", "A type of chart", "A named range only", "An error code"], answer=0),
            dict(q="What does the Total Row option add to a Table?", options=["A row with quick summary functions for each column", "A new blank table", "A chart", "A filter"], answer=0),
        ],
    ),
    dict(
        id="U8C2",
        num=2,
        title="Named Ranges (Deeper Dive)",
        notes=[
            "Beyond the Name Box method from Unit 3, Formulas > Define Name gives more control, including setting the scope (workbook-wide vs a single sheet).",
            "Workbook-scoped names can be used in any sheet's formulas; sheet-scoped names only work within that specific sheet, and can even reuse the same name across different sheets without conflict.",
            "Named ranges can refer to a fixed range, a single cell, or even a formula itself (not just raw data) - e.g. naming a formula that calculates a rolling total.",
            "Using named ranges consistently makes complex formulas dramatically easier to read for anyone reviewing the sheet later, including your future self.",
            "Name Manager (Formulas tab) is the central place to review, edit scope, or delete every named range across the whole workbook.",
        ],
        practice=[
            "Use Formulas > Define Name to create a new named range, explicitly setting its scope to the current sheet only.",
            "Create a second named range with Workbook scope and use it in a formula on a different sheet.",
            "Try creating two sheet-scoped names with the identical name on two different sheets, and confirm they don't conflict.",
            "Open Name Manager and review, then delete, one of the test names you created.",
        ],
        quiz=[
            dict(q="What extra control does Formulas > Define Name give beyond the Name Box?", options=["Setting the scope (workbook-wide or a single sheet)", "Changing the cell's font", "Adding a chart", "Sorting the range"], answer=0),
            dict(q="Can two different sheets each have their own named range using the exact same name?", options=["Yes, if both are sheet-scoped to their own sheet", "No, names must be unique across the entire workbook", "Only if one is a Table", "Only in read-only mode"], answer=0),
            dict(q="Can a named range refer to a formula instead of just raw data?", options=["Yes", "No, only fixed ranges are allowed", "Only for named ranges created via the Name Box", "Only in Excel Tables"], answer=0),
            dict(q="What is a key benefit of using named ranges in formulas?", options=["They make complex formulas easier to read and understand later", "They make formulas calculate faster", "They are required for charts to work", "They automatically sort data"], answer=0),
        ],
    ),
    dict(
        id="U8C3",
        num=3,
        title="Dynamic Arrays: Spilling Ranges Basics",
        notes=[
            "A dynamic array formula can return multiple results from a single formula entered in just one cell - the results automatically 'spill' into the neighboring cells below/right.",
            "You only write the formula once, in the top-left cell of where the results should appear - Excel handles filling the rest in automatically.",
            "A faint blue border (in Excel's default view) shows the current 'spill range' - the group of cells currently filled by one spilling formula.",
            "If something blocks a spill (like existing data sitting in a cell the results need), Excel shows a #SPILL! error until that blockage is cleared.",
            "You can reference an entire spill range in another formula using the # symbol after the top-left cell, e.g. =SUM(B2#) sums everything the formula in B2 spilled out.",
        ],
        practice=[
            "In a fresh area of a sheet, type a formula that naturally returns multiple values (you'll use UNIQUE, SORT, or FILTER for this in the next few chapters) and observe the spill behavior once you reach them.",
            "For now, type =SEQUENCE(5) in a single cell and observe 5 numbers spill downward automatically from one formula.",
            "Place some data directly in the path of where a spill would go, then try the same formula again and observe the #SPILL! error.",
            "Clear the blocking data and confirm the spill works again automatically.",
        ],
        quiz=[
            dict(q="How many cells do you need to type a dynamic array formula into?", options=["Just one - the top-left cell of where results should appear", "One per result", "The entire range in advance", "Two, minimum"], answer=0),
            dict(q="What does the faint blue border around spilled results indicate?", options=["The current spill range filled by that one formula", "An error", "A locked cell", "A named range"], answer=0),
            dict(q="What causes a #SPILL! error?", options=["Something is blocking the cells the results need to spill into", "The formula has a typo", "The range is too large for Excel", "Dividing by zero"], answer=0),
            dict(q="What does the # symbol do after a cell reference, like B2#?", options=["References the entire spill range from the formula in B2", "Comments out the formula", "Converts the value to a percentage", "Locks the reference"], answer=0),
        ],
    ),
    dict(
        id="U8C4",
        num=4,
        title="UNIQUE & SORT Functions",
        notes=[
            "UNIQUE returns a list of distinct values from a range, removing repeats automatically: =UNIQUE(A2:A20).",
            "SORT arranges a range in order: =SORT(A2:A20) sorts ascending by default; =SORT(A2:A20,1,-1) sorts descending by the first column.",
            "UNIQUE and SORT are dynamic array functions - their results spill automatically and update live if the source data changes, unlike the one-time Sort button from Unit 6.",
            "They combine well: =SORT(UNIQUE(A2:A20)) produces a distinct list in order, entered as a single formula.",
            "Because these spill and update automatically, they're excellent for building dropdown validation lists or summary sections that stay current without manual re-sorting.",
        ],
        practice=[
            "Build a column with repeated category names (like regions ordered randomly with duplicates). Use UNIQUE to list each distinct one.",
            "Use SORT on the same raw column to arrange it alphabetically without removing duplicates.",
            "Combine both: =SORT(UNIQUE(range)) in a single formula to get a sorted, distinct list.",
            "Add a new category value to the source data and confirm your combined formula's spilled results update automatically.",
        ],
        quiz=[
            dict(q="What does UNIQUE do?", options=["Returns a list of distinct values from a range", "Sorts a range", "Counts values in a range", "Removes blanks only"], answer=0),
            dict(q="How is SORT different from the one-time Sort button covered in Unit 6?", options=["SORT is a live formula that updates automatically if source data changes", "SORT only works on numbers", "SORT cannot sort descending", "There is no difference"], answer=0),
            dict(q="What does =SORT(UNIQUE(A2:A20)) produce?", options=["A sorted list of distinct values from the range", "A count of unique values", "The largest unique value only", "An error, since functions can't be nested this way"], answer=0),
            dict(q="Why are UNIQUE/SORT well suited for building dropdown validation lists?", options=["Their spilled results update automatically without manual re-sorting", "They are the only functions that work with Data Validation", "They automatically create a dropdown", "They convert text to numbers"], answer=0),
        ],
    ),
    dict(
        id="U8C5",
        num=5,
        title="The FILTER Function",
        notes=[
            "FILTER returns only the rows of a range that meet a condition, spilling the matching rows automatically: =FILTER(A2:C20, B2:B20=\"North\").",
            "Structure: =FILTER(array, include_condition, [if_empty]) - the include_condition is typically a comparison against another range of matching length.",
            "Multiple conditions can be combined using * for AND logic, or + for OR logic, e.g. =FILTER(A2:C20,(B2:B20=\"North\")*(C2:C20>100)) requires both to be true.",
            "The optional if_empty argument provides a fallback result (like \"No matches\") if the condition matches nothing, avoiding a #CALC! error.",
            "Unlike AutoFilter (Unit 6), which hides rows in place, FILTER produces a separate, live, spilled copy of just the matching rows - the original data is untouched.",
        ],
        practice=[
            "Using your sales dataset, write a FILTER formula that returns only rows where Region = 'North'.",
            "Extend the condition to also require Amount > 100, combined with * for AND logic.",
            "Add an if_empty fallback value of \"No matches\" to your formula and test it with a condition that matches nothing.",
            "Compare this FILTER result to using AutoFilter on the same data - note that FILTER creates a separate live copy while AutoFilter hides rows in place.",
        ],
        quiz=[
            dict(q="What does FILTER return?", options=["Only the rows of a range that meet a condition, spilled automatically", "A single value only", "A sorted copy of the whole range", "A count of matching rows"], answer=0),
            dict(q="How do you combine two conditions with AND logic inside FILTER?", options=["Multiply the conditions together with *", "Add them together with +", "Use a comma between them", "Use the word AND directly inside the array"], answer=0),
            dict(q="What does the if_empty argument do?", options=["Provides a fallback result if no rows match, avoiding a #CALC! error", "Skips blank rows in the source data", "Hides the formula", "Sorts the results"], answer=0),
            dict(q="How is FILTER different from AutoFilter?", options=["FILTER produces a separate live spilled copy; AutoFilter hides rows in place", "They are exactly the same", "AutoFilter is a formula; FILTER is a button", "FILTER changes the original data"], answer=0),
        ],
    ),
    dict(
        id="U8C6",
        num=6,
        title="SEQUENCE & Combining Dynamic Array Functions",
        notes=[
            "SEQUENCE generates a list of numbers: =SEQUENCE(rows, [columns], [start], [step]) - e.g. =SEQUENCE(5) gives 1 through 5 spilled down a column.",
            "SEQUENCE is handy for generating row numbers, date sequences (combined with date math), or test data quickly without typing each value.",
            "Dynamic array functions can be nested inside each other, e.g. wrapping a FILTER result inside SORT to get matching rows in order: =SORT(FILTER(A2:C20,B2:B20=\"North\")).",
            "Because these all spill and stay live, a small 'mini report' built from nested dynamic array functions updates automatically as source data grows or changes - a lightweight alternative to a Pivot Table for simple cases.",
            "As formulas nest deeper, the same rule from earlier units applies: build and test each function separately before combining them.",
        ],
        practice=[
            "Use SEQUENCE to generate the numbers 1 through 10 in a column.",
            "Use SEQUENCE with a start and step argument to generate every multiple of 5 from 5 to 50.",
            "Combine SORT and FILTER into one nested formula that returns matching rows in sorted order.",
            "Add new matching data to your source range and confirm your nested formula's results update automatically.",
        ],
        quiz=[
            dict(q="What does SEQUENCE(5) produce?", options=["The numbers 1 through 5, spilled down a column", "5 random numbers", "A 5-row blank range", "The value 5 repeated"], answer=0),
            dict(q="What do the start and step arguments in SEQUENCE control?", options=["The first number and the increment between numbers", "The number of columns only", "The formatting of the numbers", "Whether it spills or not"], answer=0),
            dict(q="What does =SORT(FILTER(A2:C20,B2:B20=\"North\")) do?", options=["Filters rows matching 'North', then sorts those matching rows", "Sorts everything, then filters afterward with no real effect", "Returns an error, since FILTER and SORT cannot nest", "Filters and sorts two different ranges independently"], answer=0),
            dict(q="Why might a nested dynamic array formula be called a lightweight alternative to a Pivot Table?", options=["It updates automatically and shows a live filtered/sorted view without manual refresh", "It is exactly as powerful as a Pivot Table in every way", "Pivot Tables cannot filter data", "Dynamic arrays require no source data"], answer=0),
        ],
    ),
]

UNIT9_CHAPTERS = [
    dict(
        id="U9C1",
        num=1,
        title="Chart Basics: Choosing the Right Chart Type",
        notes=[
            "A chart turns numbers into a visual shape, making trends, comparisons, and patterns easier to spot than scanning a table of raw numbers.",
            "Column/Bar charts compare values across categories (e.g. sales by region). Line charts show a trend over time (e.g. sales by month). Pie charts show parts of a whole (e.g. market share).",
            "Scatter charts show the relationship between two numeric variables (e.g. hours studied vs marks scored) rather than categories.",
            "Choosing the wrong chart type is a common mistake - e.g. a pie chart with 15 slices is hard to read; a line chart implies a trend over a continuous scale like time, so it shouldn't be used for unrelated categories.",
            "As a rule of thumb: comparing categories -> bar/column; trend over time -> line; parts of a whole (few categories) -> pie; relationship between two numbers -> scatter.",
        ],
        practice=[
            "Look at a dataset of monthly sales for one product over a year and decide which chart type fits best, explaining why in your own words.",
            "Look at a dataset comparing total sales across 5 different regions in one month and decide which chart type fits best.",
            "Look at a dataset with market share percentages for 4 competitors and decide if a pie chart would work well here.",
            "Look at a dataset pairing study hours with exam scores for 20 students and decide which chart type fits best.",
        ],
        quiz=[
            dict(q="Which chart type best shows a trend over time?", options=["Line chart", "Pie chart", "Scatter chart", "None of these show trends"], answer=0),
            dict(q="Which chart type best compares values across separate categories?", options=["Column/Bar chart", "Line chart", "Scatter chart", "Pie chart with 15 slices"], answer=0),
            dict(q="Which chart type shows the relationship between two numeric variables?", options=["Scatter chart", "Pie chart", "Bar chart", "Line chart"], answer=0),
            dict(q="Why is a pie chart with 15 slices considered a poor choice?", options=["Too many slices become hard to read and compare visually", "Pie charts cannot display percentages", "Pie charts only work with 2 categories", "Excel does not allow more than 5 slices"], answer=0),
        ],
    ),
    dict(
        id="U9C2",
        num=2,
        title="Creating & Formatting a Chart",
        notes=[
            "Select your data (including headers), then Insert tab > choose a chart type, or use the Quick Analysis button that appears near a selection.",
            "Once a chart exists, clicking it reveals Chart Design and Format tabs (or a Chart Elements '+' icon) for customizing it further.",
            "Chart Elements you can toggle on/off: Chart Title, Axis Titles, Data Labels, Legend, Gridlines - each adds clarity but too many at once can clutter a chart.",
            "Right-click any chart element (like an axis or a data series) for element-specific formatting options, such as changing colors or number formats.",
            "A chart can be moved to its own separate sheet (Chart Design > Move Chart > New Sheet) which is useful when a chart needs to be large and presented on its own.",
        ],
        practice=[
            "Select a small dataset (with headers) and insert a Column chart from the Insert tab.",
            "Add a Chart Title and turn on Data Labels using the Chart Elements '+' icon.",
            "Right-click one data series and change its color.",
            "Move your chart to its own new sheet using Chart Design > Move Chart.",
        ],
        quiz=[
            dict(q="Where do you insert a new chart from a selected data range?", options=["Insert tab", "Home tab", "Data tab", "Review tab"], answer=0),
            dict(q="What does the Chart Elements '+' icon let you toggle?", options=["Chart Title, Axis Titles, Data Labels, Legend, Gridlines", "The chart's data source only", "The workbook's theme", "The file format"], answer=0),
            dict(q="How do you format a single element of a chart, like one axis?", options=["Right-click that specific element", "Right-click the whole workbook", "Use Format Cells", "Use Data Validation"], answer=0),
            dict(q="Where would you move a chart to display it on its own dedicated sheet?", options=["Chart Design > Move Chart > New Sheet", "Insert > New Sheet", "File > Print", "Data > Move"], answer=0),
        ],
    ),
    dict(
        id="U9C3",
        num=3,
        title="Bar/Column Charts & Line Charts in Depth",
        notes=[
            "Column charts show bars vertically (good for comparing a handful of categories); Bar charts show them horizontally (good when category names are long, or there are many categories).",
            "A Clustered Column/Bar chart compares multiple series side by side per category; a Stacked version shows how parts contribute to a total per category.",
            "100% Stacked charts show proportions (percentages) rather than raw totals - useful for comparing composition across categories of different overall sizes.",
            "For line charts, each line typically represents one series/category tracked across a continuous axis like time - too many lines on one chart becomes cluttered and hard to follow.",
            "Markers on a line chart (small dots at each data point) can be toggled on to make individual values easier to read, especially with fewer data points.",
        ],
        practice=[
            "Build a small dataset with 3 regions' sales across 4 quarters. Create a Clustered Column chart from it.",
            "Change the same chart to a Stacked Column chart and compare how the story it tells changes.",
            "Try a 100% Stacked Column version and notice how it emphasizes proportion instead of raw totals.",
            "Create a Line chart from the same data, with one line per region, and toggle markers on.",
        ],
        quiz=[
            dict(q="What is the main difference between a Column chart and a Bar chart?", options=["Column bars are vertical, Bar chart bars are horizontal", "They are identical", "Bar charts only show one category", "Column charts cannot use color"], answer=0),
            dict(q="What does a Stacked Column chart show that a Clustered one doesn't as directly?", options=["How parts contribute to a total per category", "Individual series compared side by side", "Only percentages", "Only the largest value"], answer=0),
            dict(q="What does a 100% Stacked chart emphasize?", options=["Proportions/percentages rather than raw totals", "Only the smallest category", "Exact raw values only", "Trend over time only"], answer=0),
            dict(q="What do markers on a line chart show?", options=["Small dots highlighting each individual data point", "The chart's title", "A trend line prediction", "The legend"], answer=0),
        ],
    ),
    dict(
        id="U9C4",
        num=4,
        title="Pie Charts & When (Not) to Use Them",
        notes=[
            "A pie chart shows each category as a slice proportional to its share of the total - it only makes sense when the values genuinely add up to one meaningful whole.",
            "Pie charts work best with a small number of categories (roughly 2-6) - beyond that, slices become too thin to compare accurately by eye.",
            "A Doughnut chart is a variation of a pie chart with a hole in the middle, sometimes used to show a second ring of related data alongside the first.",
            "People are generally worse at comparing angles/areas (pie slices) than comparing bar heights or lengths - which is why a bar chart is often a clearer choice than a pie chart, even when a pie chart seems 'obvious' for percentage data.",
            "If you need to show many categories, or exact value comparisons matter more than 'share of the whole', a bar chart is usually the better choice over a pie chart.",
        ],
        practice=[
            "Build a small dataset of 4 product categories and their share of total sales, then create a pie chart from it.",
            "Add Data Labels showing percentages on the pie chart.",
            "Recreate the same data as a bar chart instead, and compare how easy each version is to read accurately.",
            "Try building a pie chart from a dataset with 12+ categories and notice how hard it becomes to compare slices.",
        ],
        quiz=[
            dict(q="When does a pie chart make the most sense?", options=["When values add up to one meaningful whole, with a small number of categories", "Whenever you have percentage data, regardless of category count", "Only for financial data", "Only when comparing two charts side by side"], answer=0),
            dict(q="Roughly how many categories work well in a pie chart?", options=["About 2-6", "Exactly 1", "20 or more", "There is no practical limit"], answer=0),
            dict(q="Why might a bar chart often be clearer than a pie chart for the same data?", options=["People compare bar lengths more accurately than pie slice angles/areas", "Bar charts always use less ink", "Pie charts cannot show percentages", "Bar charts are required by Excel for percentage data"], answer=0),
            dict(q="What is a Doughnut chart?", options=["A variation of a pie chart with a hole in the middle", "A type of line chart", "A type of scatter chart", "A chart with no legend"], answer=0),
        ],
    ),
    dict(
        id="U9C5",
        num=5,
        title="Combo Charts & Secondary Axis",
        notes=[
            "A combo chart combines two chart types in one, e.g. columns for monthly sales alongside a line for a running trend or target - Insert > Combo Chart, or Change Chart Type on an existing chart.",
            "A secondary axis is needed when two data series have very different scales (e.g. Sales in thousands vs a Conversion Rate in percent) - without it, the smaller-scale series would look flat and invisible next to the larger one.",
            "To add a secondary axis: right-click the series that needs it and choose 'Format Data Series', then select 'Secondary Axis'.",
            "Combo charts are common in dashboards for showing an actual value against a target/goal line on the same chart.",
            "Too many combined elements on one combo chart can undo its usefulness - stick to 2, occasionally 3, series maximum for readability.",
        ],
        practice=[
            "Build a dataset with monthly Sales (large numbers) and monthly Conversion Rate (a small percentage) side by side.",
            "Create a combo chart: columns for Sales, a line for Conversion Rate.",
            "Put the Conversion Rate line on a secondary axis and observe how much clearer it becomes compared to sharing the primary axis.",
            "Add a flat 'Target' line to the same chart representing a fixed goal value across all months.",
        ],
        quiz=[
            dict(q="What is a combo chart?", options=["A chart combining two different chart types in one", "A chart with no axis", "A pie chart with a legend", "A chart that cannot be edited"], answer=0),
            dict(q="When is a secondary axis needed?", options=["When two series have very different scales that would otherwise make one look flat", "Whenever you use more than one color", "Only for pie charts", "Only when using a line chart"], answer=0),
            dict(q="How do you add a secondary axis to a chart series?", options=["Right-click the series, choose Format Data Series, select Secondary Axis", "Delete the series and start over", "Use Data Validation", "Use Conditional Formatting"], answer=0),
            dict(q="What is a common dashboard use case for a combo chart?", options=["Showing an actual value against a target/goal line", "Showing only text labels", "Replacing a pivot table entirely", "Hiding data from viewers"], answer=0),
        ],
    ),
    dict(
        id="U9C6",
        num=6,
        title="Sparklines",
        notes=[
            "A Sparkline is a tiny chart that fits inside a single cell, showing a trend at a glance without taking up the space of a full chart - Insert tab > Sparklines.",
            "Three types: Line (trend), Column (bar-style per point), and Win/Loss (shows only up/down, ignoring magnitude - handy for pass/fail or profit/loss streaks).",
            "Sparklines are placed directly in a cell, commonly right next to a row of raw data they summarize, e.g. one Sparkline per product row showing its 12-month trend.",
            "Sparkline Design tab (appears when a sparkline is selected) lets you highlight the high point, low point, first/last points, and change style/color.",
            "Because they're so compact, Sparklines are popular in dashboards or tables where many trends need to be scanned quickly, row by row.",
        ],
        practice=[
            "Build a small table with one row per product and 6 months of sales data across columns.",
            "Insert a Line Sparkline in a new column, summarizing each row's 6-month trend.",
            "Change one product's Sparkline to a Column type and compare readability.",
            "Use Sparkline Design options to highlight the high and low points on one of your sparklines.",
        ],
        quiz=[
            dict(q="What is a Sparkline?", options=["A tiny chart that fits inside a single cell", "A full-size chart on its own sheet", "A type of pivot table", "A named range"], answer=0),
            dict(q="Which Sparkline type shows only up/down movement, ignoring magnitude?", options=["Win/Loss", "Line", "Column", "Scatter"], answer=0),
            dict(q="Where are Sparklines typically placed?", options=["Directly in a cell, often next to the row of data they summarize", "On a separate chart sheet only", "In the header row only", "In the Name Box"], answer=0),
            dict(q="What can the Sparkline Design tab help highlight?", options=["High point, low point, and first/last points", "Only the chart title", "Only the legend", "Only gridlines"], answer=0),
        ],
    ),
    dict(
        id="U9C7",
        num=7,
        title="Dashboard Layout Basics",
        notes=[
            "A dashboard is a single sheet combining several charts, key numbers (KPIs), and sometimes small tables, designed to be scanned quickly for a full picture.",
            "A common layout: key summary numbers (KPIs) across the top, main trend charts in the middle, supporting detail/breakdown charts below or to the side.",
            "Keep a consistent color scheme across all charts on a dashboard - using a different color for the same category in different charts confuses readers.",
            "Freeze Panes and hiding gridlines (View tab > Gridlines checkbox) can make a dashboard sheet feel cleaner and more presentation-ready, closer to a real report than a raw spreadsheet.",
            "Since this course doesn't cover Pivot Tables until Unit 11, an early dashboard can be built entirely from formulas (SUMIFS, COUNTIFS) and charts covered so far - Unit 11 will revisit dashboards using Pivot Tables and slicers for a more powerful version.",
        ],
        practice=[
            "On a new sheet, use SUMIFS/COUNTIFS formulas (from Unit 5) to calculate 3-4 key summary numbers from your sales dataset.",
            "Add 2 charts below those numbers: one comparing categories, one showing a trend over time.",
            "Apply a consistent color scheme across both charts.",
            "Turn off gridlines on this sheet (View tab) to make it feel more like a clean report.",
        ],
        quiz=[
            dict(q="What is a dashboard, in this context?", options=["A single sheet combining charts, KPIs, and tables for a quick overview", "A single very large chart", "A hidden worksheet", "A type of formula"], answer=0),
            dict(q="Why keep a consistent color scheme across dashboard charts?", options=["Using different colors for the same category across charts confuses readers", "Excel requires it", "It makes formulas calculate faster", "It is required for printing"], answer=0),
            dict(q="What is a common dashboard layout pattern mentioned in this chapter?", options=["KPIs across the top, main trend charts in the middle, details below", "Everything randomly placed for variety", "One giant pie chart only", "Charts hidden behind a button"], answer=0),
            dict(q="What will Unit 11 revisit dashboards with, that this early version doesn't yet use?", options=["Pivot Tables and slicers", "VBA macros", "Power Query", "Data Validation"], answer=0),
        ],
    ),
]

UNIT10_CHAPTERS = [
    dict(
        id="U10C1",
        num=1,
        title="Goal Seek",
        notes=[
            "Goal Seek (Data tab > What-If Analysis > Goal Seek) works backward: you tell Excel the result you WANT from a formula, and it finds the input value that produces it.",
            "You set 3 things: 'Set cell' (the formula cell), 'To value' (the target result you want), and 'By changing cell' (the input Excel should adjust).",
            "Example: if a loan payment formula shows a monthly payment too high, Goal Seek can find what loan amount would bring the payment down to a specific target.",
            "Goal Seek only adjusts ONE input cell at a time, and that cell must feed into the formula in the 'Set cell', directly or indirectly.",
            "Goal Seek changes the actual value in the 'By changing cell' - it's a real edit, not just a preview, so undo (Ctrl+Z) if you want to revert it.",
        ],
        practice=[
            "Build a simple formula: Price * Quantity = Total, with Price and Quantity in separate cells.",
            "Use Goal Seek to find what Quantity would be needed to reach a specific Total, keeping Price fixed.",
            "Undo the Goal Seek result and try again targeting a different Total value.",
            "Try using Goal Seek on the Price cell instead, to reach the same Total with a fixed Quantity.",
        ],
        quiz=[
            dict(q="What does Goal Seek do?", options=["Finds the input value needed to reach a target formula result", "Automatically formats a cell", "Sorts a range", "Creates a chart"], answer=0),
            dict(q="How many input cells can Goal Seek adjust at once?", options=["Only one", "Two", "As many as needed", "Zero, it only previews"], answer=0),
            dict(q="What three things do you set in the Goal Seek dialog?", options=["Set cell, To value, By changing cell", "Start cell, End cell, Step", "Range, Criteria, Result", "Formula, Value, Format"], answer=0),
            dict(q="Does Goal Seek permanently change the value in the 'By changing cell'?", options=["Yes, it's a real edit that can be undone with Ctrl+Z", "No, it only shows a preview", "It changes formatting only", "It creates a new sheet with the result"], answer=0),
        ],
    ),
    dict(
        id="U10C2",
        num=2,
        title="Data Tables (One-Variable & Two-Variable)",
        notes=[
            "A Data Table (Data tab > What-If Analysis > Data Table) shows how a formula's result changes across a whole range of different input values at once, without rebuilding the formula each time.",
            "A one-variable data table tests different values for ONE input (e.g. different interest rates) and shows the resulting output (e.g. monthly payment) for each.",
            "A two-variable data table tests combinations of TWO inputs at once (e.g. different interest rates AND different loan terms), producing a grid of results.",
            "The formula being tested must be linked to the input cells the Data Table references - the table itself is set up in a specific grid layout Excel expects (inputs listed down a column or across a row, with the formula referenced in a specific corner cell).",
            "Data Tables are useful for building a quick 'what if this input were different' reference grid, like a loan payment table across several rate/term combinations, without writing dozens of separate formulas.",
        ],
        practice=[
            "Build a simple loan payment formula using cells for Rate, Term, and Amount (you'll use the actual PMT function in a later chapter - for now a placeholder formula is fine).",
            "Set up a one-variable Data Table listing 5 different interest rates down a column, showing the resulting payment for each.",
            "Extend it into a two-variable Data Table by adding a row of different loan terms across the top as well.",
            "Change the base Amount cell and confirm the whole Data Table grid recalculates.",
        ],
        quiz=[
            dict(q="What does a one-variable Data Table test?", options=["Different values for one input, showing the result for each", "Two inputs at once", "No inputs, just the current formula", "Only text values"], answer=0),
            dict(q="What does a two-variable Data Table produce?", options=["A grid of results from combinations of two different inputs", "A single result", "A chart", "A sorted list"], answer=0),
            dict(q="What must be true about the formula being tested in a Data Table?", options=["It must be linked to the input cells the table references", "It must be a text formula", "It cannot use any functions", "It must be on a separate sheet"], answer=0),
            dict(q="What is a Data Table useful for?", options=["Building a quick reference grid of results across many input combinations", "Formatting cells automatically", "Removing duplicates", "Creating a pivot table"], answer=0),
        ],
    ),
    dict(
        id="U10C3",
        num=3,
        title="Scenario Manager",
        notes=[
            "Scenario Manager (Data tab > What-If Analysis > Scenario Manager) lets you save several different sets of input values ('scenarios') and switch between them to see how results change.",
            "Example: a 'Best Case', 'Worst Case', and 'Likely Case' scenario, each setting different values for Sales Growth and Costs, all feeding the same summary formulas.",
            "Unlike Data Tables (which vary 1-2 inputs automatically across a range), Scenario Manager lets you name and control several inputs together as one named, saved combination.",
            "A Scenario Summary report (button inside Scenario Manager) generates a new sheet comparing all your saved scenarios and their results side by side.",
            "Switching scenarios changes the actual input cell values in the workbook, just like Goal Seek does - so save your workbook, or note original values, before experimenting extensively.",
        ],
        practice=[
            "Build a small forecast: Sales Growth % and Cost Increase % feeding a Projected Profit formula.",
            "Create a 'Best Case' scenario with optimistic values for both inputs.",
            "Create a 'Worst Case' scenario with pessimistic values for both inputs.",
            "Use the Scenario Summary report to compare both scenarios' results side by side on a new sheet.",
        ],
        quiz=[
            dict(q="What does Scenario Manager let you do?", options=["Save and switch between several named sets of input values", "Sort a range of scenarios", "Automatically forecast future values", "Create a chart of historical data"], answer=0),
            dict(q="How is Scenario Manager different from a Data Table?", options=["It lets you name and control several inputs together, rather than auto-varying 1-2 inputs across a range", "It cannot handle more than one input", "It only works with text", "It is identical to a Data Table"], answer=0),
            dict(q="What does the Scenario Summary report do?", options=["Generates a new sheet comparing all saved scenarios side by side", "Deletes all scenarios", "Merges all scenarios into one", "Prints the current sheet"], answer=0),
            dict(q="Does switching scenarios change the actual values in the workbook's input cells?", options=["Yes, just like Goal Seek", "No, it only shows a preview", "Only in Scenario Summary reports", "Only if AutoSave is off"], answer=0),
        ],
    ),
    dict(
        id="U10C4",
        num=4,
        title="Basic Financial Functions: PMT, FV & PV",
        notes=[
            "PMT calculates a loan's regular payment amount: =PMT(rate, nper, pv) - rate is the period interest rate, nper is the number of payment periods, pv is the loan amount (present value).",
            "The interest rate must match the payment frequency - e.g. for a monthly payment on an annual rate, divide the annual rate by 12 inside the formula.",
            "FV (Future Value) calculates what a series of regular investments/savings will grow to: =FV(rate, nper, pmt) - useful for projecting savings growth over time.",
            "PV (Present Value) calculates what a future amount is worth today, or what loan amount a given payment can afford: =PV(rate, nper, pmt).",
            "PMT and PV results often show as negative numbers by convention (representing money paid out) - this is expected Excel behavior, not an error.",
        ],
        practice=[
            "Use PMT to calculate the monthly payment on a loan of 100000 at a 10% annual rate over 5 years (remember to convert the annual rate and years into monthly terms).",
            "Use FV to calculate what saving a fixed amount monthly at a given rate would grow to after 10 years.",
            "Use PV to calculate what loan amount a specific fixed monthly payment could afford, given a rate and term.",
            "Note whether your PMT/PV results appear negative, and explain in your own words why that happens.",
        ],
        quiz=[
            dict(q="What does the PMT function calculate?", options=["A loan's regular payment amount", "The total interest paid", "The number of payments left", "The loan's interest rate"], answer=0),
            dict(q="Why must an annual interest rate often be divided by 12 inside these formulas?", options=["To match the rate to a monthly payment frequency", "Excel requires all rates to be small decimals", "It converts the rate to a percentage", "It is only needed for FV, not PMT"], answer=0),
            dict(q="What does FV calculate?", options=["What a series of regular investments will grow to in the future", "What a future amount is worth today", "A monthly loan payment", "An interest rate"], answer=0),
            dict(q="Why do PMT and PV results often display as negative numbers?", options=["By convention, representing money paid out", "Because the formula has an error", "Because the rate was entered incorrectly", "Because FV always returns negative"], answer=0),
        ],
    ),
    dict(
        id="U10C5",
        num=5,
        title="Percentage Growth & Compound Interest Formulas",
        notes=[
            "Percentage change between two values: =(New-Old)/Old, often formatted as a Percentage. A positive result means growth, negative means decline.",
            "Compound interest/growth over multiple periods: =Principal*(1+rate)^periods - each period's growth applies to the previous period's already-grown amount, not just the original principal.",
            "This differs from simple interest, which only ever applies to the original principal: =Principal*(1+rate*periods) - compound growth accelerates faster over time.",
            "A common real-world use: projecting how a fixed starting amount grows year over year at a steady growth rate, like a savings account or an investment estimate.",
            "These formulas connect directly to FV from the previous chapter - FV is essentially a built-in function for this same compound growth idea, extended to handle regular contributions too.",
        ],
        practice=[
            "Given an Old and New sales figure, calculate the percentage change between them and format it as a Percentage.",
            "Calculate compound growth: given a Principal of 10000 growing at 8% per year, find the value after 5 years using the compound formula.",
            "Calculate the same scenario using simple interest instead, and compare the two results - note how much larger the compound result is.",
            "Cross-check your compound growth formula result against an FV formula for the same principal, rate, and periods (with no regular payments).",
        ],
        quiz=[
            dict(q="What is the formula for percentage change between an Old and New value?", options=["(New-Old)/Old", "(Old-New)/New", "New/Old", "Old-New"], answer=0),
            dict(q="What makes compound growth different from simple growth?", options=["Each period's growth applies to the already-grown amount, not just the original principal", "Compound growth only applies once", "Simple growth grows faster over time", "They are mathematically identical"], answer=0),
            dict(q="What is the formula structure for compound growth over multiple periods?", options=["Principal*(1+rate)^periods", "Principal*(1+rate*periods)", "Principal/(1+rate)^periods", "Principal*rate*periods"], answer=0),
            dict(q="Which earlier function is essentially a built-in version of this same compound growth idea?", options=["FV (Future Value)", "PMT", "VLOOKUP", "SUMIFS"], answer=0),
        ],
    ),
    dict(
        id="U10C6",
        num=6,
        title="Building a Simple Loan/Budget Calculator",
        notes=[
            "A practical calculator combines several concepts from this unit into one working tool: input cells (rate, amount, term) feeding formulas (PMT, FV) that update live as inputs change.",
            "Clearly labeling and separating 'Input' cells from 'Result' cells (perhaps with light shading from Unit 2's formatting skills) makes a calculator easier for someone else to use correctly.",
            "Data Validation (Unit 5) can restrict inputs to sensible ranges, e.g. preventing a negative interest rate or an unrealistic loan term from being typed in by mistake.",
            "Adding a small Data Table (from earlier in this unit) alongside the main calculator lets a user see several rate/term scenarios at a glance without re-typing anything.",
            "This kind of small, self-contained, clearly-labeled tool is exactly the pattern used again at a larger scale in the Capstone project (Unit 14).",
        ],
        practice=[
            "Build a Loan Calculator sheet with labeled Input cells: Loan Amount, Annual Rate, Term (years).",
            "Add a Result cell using PMT to calculate the monthly payment from those inputs.",
            "Apply Data Validation to the rate input to prevent negative or unrealistic values.",
            "Add a one-variable Data Table showing the monthly payment across 4-5 different interest rates.",
        ],
        quiz=[
            dict(q="Why separate and clearly label Input cells from Result cells in a calculator?", options=["It makes the tool easier for someone else to use correctly", "Excel requires this structure to calculate", "It prevents all formula errors automatically", "It is only useful for printing"], answer=0),
            dict(q="What can Data Validation add to a calculator's input cells?", options=["Restricting inputs to sensible ranges, preventing unrealistic values", "Automatic formatting only", "A chart", "A pivot table"], answer=0),
            dict(q="What does adding a Data Table alongside a calculator let a user do?", options=["See several scenarios at a glance without re-typing inputs", "Delete the calculator's formulas", "Automatically email the results", "Convert the calculator into a chart"], answer=0),
            dict(q="Where does this course revisit this same 'small, labeled, self-contained tool' pattern at a larger scale?", options=["The Capstone project in Unit 14", "Unit 1", "Unit 3", "Unit 6"], answer=0),
        ],
    ),
]

UNIT11_CHAPTERS = [
    dict(
        id="U11C1",
        num=1,
        title="Pivot Table Basics: Creating One",
        notes=[
            "A Pivot Table summarizes large datasets by grouping and aggregating them interactively, without writing any formulas - Insert tab > PivotTable.",
            "Excel needs 'clean' source data to build a good pivot table: consistent column headers, no blank rows/columns within the data, and one row per record (very similar to the cleaning habits from Unit 6).",
            "Once created, a Pivot Table starts empty with a Field List panel on the side, listing every column from your source data as a draggable 'field'.",
            "You build the summary by dragging fields into four areas: Rows, Columns, Values, and Filters - the Pivot Table updates live as you rearrange them.",
            "A Pivot Table is a separate object from the source data - it does not alter your original data, and won't update automatically if the source changes until you click Refresh (Pivot Table Analyze tab).",
        ],
        practice=[
            "Using a clean sales dataset (Region, Product, Amount, Date columns), select it and insert a Pivot Table.",
            "Drag Region into the Rows area and Amount into the Values area - observe the automatic summary.",
            "Add Product into the Columns area to break the summary down further.",
            "Change a value in your source data, then click Refresh on the Pivot Table and confirm it updates.",
        ],
        quiz=[
            dict(q="What does a Pivot Table let you do without writing formulas?", options=["Summarize large datasets by grouping and aggregating them interactively", "Automatically clean messy data", "Create charts only", "Sort data permanently"], answer=0),
            dict(q="What kind of source data does a Pivot Table need to work well?", options=["Clean data with consistent headers and no blank rows within it", "Any data, formatting doesn't matter", "Only numeric data", "Data must already be sorted"], answer=0),
            dict(q="What are the four areas you drag fields into when building a Pivot Table?", options=["Rows, Columns, Values, Filters", "Top, Bottom, Left, Right", "Input, Output, Formula, Result", "Header, Body, Footer, Total"], answer=0),
            dict(q="Does a Pivot Table update automatically the instant source data changes?", options=["No, you need to click Refresh", "Yes, instantly and automatically", "Only if AutoSave is on", "Only for numeric changes"], answer=0),
        ],
    ),
    dict(
        id="U11C2",
        num=2,
        title="Rows, Columns, Values & Filters in Depth",
        notes=[
            "The Rows area creates one row per unique value of the field(s) placed there - placing more than one field nests them, creating a grouped hierarchy.",
            "The Columns area works the same way but spreads unique values across the top as column headers instead of down the side.",
            "The Values area aggregates a field (Sum, Count, Average, etc. - covered in depth next chapter) for each Row/Column combination.",
            "The Filters area lets you narrow the whole Pivot Table down to specific values of a field without putting that field into Rows or Columns directly, e.g. filtering the entire table to just one Year.",
            "Fields can be dragged between areas at any time, or removed by dragging them back out of the layout area, letting you reshape the same summary many different ways quickly.",
        ],
        practice=[
            "Put Region in Rows and Product also in Rows (below Region) and observe the nested grouping.",
            "Move Product from Rows into Columns instead, and compare the resulting layout.",
            "Add a Year (or Date) field into the Filters area and use it to narrow the table to one specific period.",
            "Practice dragging fields in and out of different areas quickly to reshape the same summary 3-4 different ways.",
        ],
        quiz=[
            dict(q="What happens when you place two fields into the Rows area?", options=["They nest, creating a grouped hierarchy", "Only the first field is used", "It causes an error", "They merge into one field"], answer=0),
            dict(q="What does the Filters area let you do?", options=["Narrow the whole Pivot Table to specific values of a field", "Delete fields permanently", "Sort the source data", "Change number formats"], answer=0),
            dict(q="What is the difference between the Rows area and the Columns area?", options=["Rows list unique values down the side; Columns spread them across the top", "They are functionally identical", "Columns can only hold numbers", "Rows can only hold one field ever"], answer=0),
            dict(q="Can fields be moved between Rows, Columns, Values, and Filters after the Pivot Table is built?", options=["Yes, at any time", "No, the layout is fixed once created", "Only Values can be changed", "Only by rebuilding the whole table"], answer=0),
        ],
    ),
    dict(
        id="U11C3",
        num=3,
        title="Value Field Settings & Number Formatting",
        notes=[
            "By default, a numeric field dropped into Values sums it; a text field dropped into Values counts it - but this can be changed.",
            "Click the dropdown on a Values field (or right-click it) > 'Value Field Settings' to switch between Sum, Count, Average, Max, Min, and other summary types.",
            "'Show Values As' (inside Value Field Settings) offers more advanced views, like '% of Grand Total' or '% of Column Total', useful for showing proportions instead of raw numbers.",
            "Number Format button (inside Value Field Settings) applies proper formatting (like Currency or Percentage) directly to the Pivot Table's values, separate from the source data's formatting.",
            "Renaming a Values field's header (e.g. 'Sum of Amount' to 'Total Sales') makes the Pivot Table more presentable - just click into the header cell and type a new name.",
        ],
        practice=[
            "With Amount in the Values area, open Value Field Settings and switch the summary from Sum to Average.",
            "Switch it back to Sum, then apply Currency number formatting directly through Value Field Settings.",
            "Try 'Show Values As > % of Grand Total' and observe how the numbers change to proportions.",
            "Rename the Values field header from the default 'Sum of Amount' to something clearer, like 'Total Sales'.",
        ],
        quiz=[
            dict(q="What does a text field default to when placed in the Values area?", options=["Count", "Sum", "Average", "Max"], answer=0),
            dict(q="Where do you change a Values field from Sum to Average?", options=["Value Field Settings", "Format Cells", "Data Validation", "Conditional Formatting"], answer=0),
            dict(q="What does 'Show Values As > % of Grand Total' do?", options=["Shows each value as a proportion of the overall total instead of a raw number", "Deletes the raw numbers", "Sorts the values", "Changes the chart type"], answer=0),
            dict(q="How do you rename a Values field header like 'Sum of Amount'?", options=["Click into the header cell and type a new name", "It cannot be renamed", "Delete and rebuild the whole Pivot Table", "Use Find & Replace only"], answer=0),
        ],
    ),
    dict(
        id="U11C4",
        num=4,
        title="Grouping Data (Dates & Numbers)",
        notes=[
            "Right-clicking a date field in the Rows/Columns area and choosing 'Group' lets you roll individual dates up into Months, Quarters, or Years automatically.",
            "Grouping numeric fields works similarly - right-click a number in Rows/Columns and choose 'Group' to bucket continuous values into ranges, e.g. Age grouped into 0-18, 19-35, 36-50, etc.",
            "Grouping is especially useful for date-heavy datasets, turning hundreds of individual daily records into a clean Month-by-Month or Quarter-by-Quarter summary instantly.",
            "Grouped fields can still be expanded/collapsed by clicking the small +/- icons next to each group, letting a viewer drill from a Year down into its Months on demand.",
            "'Ungroup' (same right-click menu) reverses grouping if you want to go back to the original individual values.",
        ],
        practice=[
            "Using a dataset with individual daily/transaction dates, put Date into Rows and Amount into Values.",
            "Right-click a date in the Pivot Table and Group it by Month and Year.",
            "Try collapsing and expanding a Year group to drill into its Months.",
            "Build a second Pivot Table grouping a numeric field (like Age or Amount) into custom ranges.",
        ],
        quiz=[
            dict(q="What does grouping a date field let you do?", options=["Roll individual dates up into Months, Quarters, or Years", "Delete the date field", "Convert dates to text", "Sort dates alphabetically"], answer=0),
            dict(q="How do you group a date or numeric field in a Pivot Table?", options=["Right-click it and choose Group", "Use Data Validation", "Use Conditional Formatting", "It groups automatically with no action needed"], answer=0),
            dict(q="What do the small +/- icons next to a grouped field let a viewer do?", options=["Expand or collapse the group, e.g. drilling from Year into Months", "Delete the group permanently", "Change the number format", "Add a new field"], answer=0),
            dict(q="What does Ungroup do?", options=["Reverses grouping, returning to the original individual values", "Deletes the Pivot Table", "Groups by a different field automatically", "Converts values to percentages"], answer=0),
        ],
    ),
    dict(
        id="U11C5",
        num=5,
        title="Pivot Charts",
        notes=[
            "A Pivot Chart is a chart linked directly to a Pivot Table's data - PivotTable Analyze tab > PivotChart (or Insert tab once a Pivot Table cell is selected).",
            "Unlike a regular chart, a Pivot Chart stays interactive: changing the underlying Pivot Table's layout (e.g. adding a filter, changing grouping) updates the chart automatically too.",
            "Pivot Charts get their own Field Buttons (small interactive dropdowns directly on the chart) letting a viewer filter the chart without touching the Pivot Table itself.",
            "Most standard chart types from Unit 9 (column, line, pie, bar) are available for Pivot Charts, and the same chart-choice guidance from that unit still applies.",
            "Field Buttons can be hidden (PivotChart Analyze/Format tab) for a cleaner, more presentation-ready look once the chart's filtering needs are finalized.",
        ],
        practice=[
            "From your Region/Product Pivot Table, insert a Pivot Chart summarizing Amount by Region.",
            "Use the Field Buttons directly on the chart to filter it down to one Product.",
            "Change the underlying Pivot Table's grouping or layout and confirm the Pivot Chart updates automatically.",
            "Hide the Field Buttons on the chart for a cleaner look once you're done experimenting.",
        ],
        quiz=[
            dict(q="How is a Pivot Chart different from a regular chart?", options=["It stays linked to the Pivot Table and updates automatically when the table's layout changes", "It cannot be formatted", "It only shows text labels", "It requires no source data"], answer=0),
            dict(q="What do Field Buttons on a Pivot Chart let a viewer do?", options=["Filter the chart directly without touching the Pivot Table", "Change the chart's colors only", "Delete the underlying data", "Export the chart to another file"], answer=0),
            dict(q="Where would you insert a Pivot Chart from an existing Pivot Table?", options=["PivotTable Analyze tab, or Insert tab with a Pivot Table cell selected", "Data tab only", "Page Layout tab", "Review tab"], answer=0),
            dict(q="Can Field Buttons be hidden for a cleaner presentation?", options=["Yes, via the PivotChart Analyze/Format tab", "No, they are always visible", "Only by deleting the chart", "Only in printed versions"], answer=0),
        ],
    ),
    dict(
        id="U11C6",
        num=6,
        title="Slicers & Timelines",
        notes=[
            "A Slicer is a clickable button panel for filtering a Pivot Table (or Pivot Chart) visually - Insert tab > Slicer, once a Pivot Table cell is selected.",
            "Unlike the Filters area dropdown, a Slicer shows every option as a visible button at once, making it obvious at a glance what's currently selected or available.",
            "One Slicer can be connected to multiple Pivot Tables/Charts at once (Slicer > Report Connections), so a single click filters everything on a dashboard together.",
            "A Timeline is a Slicer specifically for date fields, showing a scrollable strip you can drag across to select a date range, with quick zoom levels (Days/Months/Quarters/Years).",
            "Slicers and Timelines can be resized, repositioned, and formatted with different colors/styles, just like any other object on a sheet - key for building a clean dashboard.",
        ],
        practice=[
            "Insert a Slicer for the Region field on your Pivot Table.",
            "Click through a few Region buttons on the Slicer and watch the Pivot Table filter live.",
            "Insert a Timeline for your Date field and drag across a specific range to filter by date.",
            "If you have two Pivot Tables from the same source data, connect one Slicer to both via Report Connections.",
        ],
        quiz=[
            dict(q="What does a Slicer provide that the Filters area dropdown doesn't as clearly?", options=["Every option visible as a clickable button at once", "The ability to filter numeric fields only", "Automatic sorting", "A chart of the filtered data"], answer=0),
            dict(q="What does Report Connections let a Slicer do?", options=["Connect to and filter multiple Pivot Tables/Charts at once", "Connect to an external database", "Export data to another file", "Change the source data"], answer=0),
            dict(q="What is a Timeline specifically designed to filter?", options=["Date fields, via a scrollable date-range strip", "Text fields", "Numeric ranges", "Region names"], answer=0),
            dict(q="What quick zoom levels does a Timeline typically offer?", options=["Days, Months, Quarters, Years", "Hours, Minutes, Seconds", "Rows, Columns, Values", "Small, Medium, Large"], answer=0),
        ],
    ),
    dict(
        id="U11C7",
        num=7,
        title="Building a One-Page Pivot Dashboard",
        notes=[
            "A Pivot-powered dashboard combines several Pivot Tables/Charts, Slicers, and Timelines on one sheet, all typically connected to the same underlying source data.",
            "A clean layout approach: place Slicers/Timelines along the top or one side for easy access, with Pivot Charts arranged in a grid below or beside them.",
            "Connecting one set of Slicers to every Pivot Table/Chart on the dashboard (via Report Connections) makes the whole page respond together to a single click - this is the payoff of everything built in this unit.",
            "Hiding gridlines, hiding the underlying Pivot Table's raw grid where a chart already tells the story, and consistent chart colors (from Unit 9) all make this feel like a polished dashboard rather than a raw worksheet.",
            "This Pivot-based dashboard is a more powerful version of the formula-based dashboard built at the end of Unit 9 - both are valid approaches depending on whether the data needs interactive drill-down (Pivot) or a simpler, fixed view (formulas).",
        ],
        practice=[
            "On a new sheet, build 2 Pivot Charts from your sales data: one by Region, one by Product.",
            "Add a Slicer for Region and a Timeline for Date, both connected to both Pivot Charts via Report Connections.",
            "Arrange everything into a clean grid layout: Slicer/Timeline on top, charts below.",
            "Hide gridlines on this sheet and click through your Slicer/Timeline to confirm the whole dashboard updates together.",
        ],
        quiz=[
            dict(q="What does connecting one set of Slicers to every chart on a dashboard achieve?", options=["The whole page responds together to a single click", "It merges all the Pivot Tables into one", "It disables filtering entirely", "It hides all the charts"], answer=0),
            dict(q="What is a suggested layout approach for a Pivot dashboard?", options=["Slicers/Timelines along the top or side, charts arranged in a grid below/beside them", "Everything stacked randomly in one column", "Only text, no charts", "One giant Pivot Table with no charts"], answer=0),
            dict(q="How does this Pivot-based dashboard compare to the formula-based one from Unit 9?", options=["It's a more powerful, interactive version; the formula-based one is simpler and fixed", "They are functionally identical in every way", "The formula-based one is always better", "Pivot dashboards cannot include charts"], answer=0),
            dict(q="What helps a dashboard sheet feel polished rather than like a raw worksheet?", options=["Hiding gridlines and using consistent chart colors", "Adding as many colors as possible", "Showing every raw Pivot Table grid alongside every chart", "Removing all Slicers"], answer=0),
        ],
    ),
]

UNIT12_CHAPTERS = [
    dict(
        id="U12C1",
        num=1,
        title="What is Power Query & Why Use It",
        notes=[
            "Power Query is Excel's built-in tool for importing, cleaning, and reshaping data BEFORE it lands in your worksheet - Data tab > Get Data (or Get & Transform Data group).",
            "Unlike manual cleaning (Unit 6), Power Query records every step you take as a reusable, editable recipe - if the source data changes or grows, you click Refresh and every step re-runs automatically.",
            "This 'recorded steps' approach means Power Query is repeatable and auditable - you (or someone else) can see exactly what transformations were applied, in order, and adjust any step later.",
            "Power Query has its own separate editor window (the Power Query Editor) with its own ribbon, distinct from the main Excel window - changes there don't affect your original source file.",
            "A typical Power Query workflow: connect to a data source, clean/transform it in the Query Editor, then load the result into a worksheet or the Data Model as a ready-to-use table.",
        ],
        practice=[
            "Open the Data tab and locate the Get & Transform Data group (or Get Data menu) to see what source types are available.",
            "In your own words, write down the difference between manually cleaning data (Unit 6) and cleaning it through Power Query.",
            "Think of one dataset you deal with regularly (school marks, personal expenses, etc.) that gets updated periodically, and note why a repeatable Power Query process might help there.",
            "Look at the Home tab inside a Power Query Editor window (open one via Get Data > From Table/Range on any small dataset) and identify 2-3 buttons that look familiar from regular Excel.",
        ],
        quiz=[
            dict(q="What does Power Query let you do before data lands in your worksheet?", options=["Import, clean, and reshape it", "Only chart it", "Only print it", "Only email it"], answer=0),
            dict(q="What is the key advantage of Power Query's 'recorded steps' approach?", options=["It's repeatable - refreshing re-runs every step automatically if source data changes", "It deletes the original data permanently", "It requires no source data at all", "It only works once and can't be reused"], answer=0),
            dict(q="Where do Power Query transformations happen?", options=["In a separate Power Query Editor window", "Directly in the worksheet cells", "In the Name Manager", "In Conditional Formatting"], answer=0),
            dict(q="What is a typical Power Query workflow?", options=["Connect to a source, transform it in the editor, then load the result into the worksheet", "Type formulas directly into cells", "Only sort and filter with AutoFilter", "Record a macro instead"], answer=0),
        ],
    ),
    dict(
        id="U12C2",
        num=2,
        title="Importing Data: From a Table/Range & From CSV",
        notes=[
            "Data > Get Data > From Table/Range (or 'From Sheet') turns an existing Excel range/Table into a Power Query, launching the Query Editor with that data loaded in.",
            "Data > Get Data > From File > From Text/CSV connects to an external CSV file, showing a preview before you commit to loading or transforming it.",
            "During CSV import, Power Query lets you confirm the delimiter and preview how columns will split, similar in spirit to Text to Columns (Unit 6) but happening at import time.",
            "Once connected, 'Transform Data' opens the Query Editor to clean things up first; 'Load' skips straight to placing the data into the worksheet as-is.",
            "A query connected to an external CSV file can be refreshed later to pull in that file's latest contents, without you re-importing manually each time.",
        ],
        practice=[
            "Select a small existing table/range in your workbook and use Get Data > From Table/Range to open it in the Query Editor.",
            "Close that editor without loading changes (Close & Load can be cancelled/discarded), just to practice opening and closing safely.",
            "If you have a CSV file available, use Get Data > From Text/CSV to preview its contents before importing.",
            "Note one difference you observed between the CSV import preview and simply opening a CSV file directly in Excel.",
        ],
        quiz=[
            dict(q="What does Get Data > From Table/Range do?", options=["Turns an existing Excel range/Table into a Power Query", "Deletes the existing range", "Creates a Pivot Table directly", "Converts the range to a chart"], answer=0),
            dict(q="What does the CSV import preview let you confirm before loading?", options=["The delimiter and how columns will split", "The file's creation date", "The number of macros in the file", "The chart type"], answer=0),
            dict(q="What is the difference between 'Transform Data' and 'Load' during import?", options=["Transform Data opens the Query Editor first; Load places the data directly into the worksheet", "They do the exact same thing", "Load deletes the source file", "Transform Data skips the editor entirely"], answer=0),
            dict(q="Can a query connected to an external CSV file be refreshed to pull in newer data later?", options=["Yes", "No, it's a one-time snapshot only", "Only if the CSV is renamed", "Only within the same Excel session"], answer=0),
        ],
    ),
    dict(
        id="U12C3",
        num=3,
        title="Basic Transformations: Remove Columns, Change Types & Filter Rows",
        notes=[
            "Inside the Query Editor, right-click any column header to Remove, Rename, Duplicate, or change that column's Data Type (Text, Whole Number, Date, etc.).",
            "Power Query is strict about data types - a column typed as 'Text' won't behave like a number in later steps, so setting correct types early prevents downstream errors.",
            "Filter Rows works from small dropdown arrows on column headers, just like AutoFilter in regular Excel (Unit 6), but as a permanent, repeatable step in the query.",
            "The Applied Steps panel (right side of the Query Editor) lists every transformation in order - clicking any step shows the data exactly as it looked at that point, and steps can be reordered, edited, or deleted.",
            "Removing columns you don't need early keeps the query lighter and the later steps clearer to read in the Applied Steps list.",
        ],
        practice=[
            "Open a query on a sample dataset and remove one column you don't need using the right-click menu.",
            "Change the data type of a date-like column explicitly to Date, and a number-like column explicitly to Whole Number.",
            "Use the column header filter dropdown to filter out rows matching a condition, just like AutoFilter.",
            "Click through 2-3 different steps in the Applied Steps panel and observe how the data preview changes at each stage.",
        ],
        quiz=[
            dict(q="Why does setting correct data types early in Power Query matter?", options=["A column typed incorrectly won't behave correctly in later transformation steps", "It changes the file format", "It is purely cosmetic", "It only matters for CSV files"], answer=0),
            dict(q="What does the Applied Steps panel show?", options=["Every transformation applied, in order, each one clickable to preview", "Only the final result", "A list of chart types", "A list of macros"], answer=0),
            dict(q="How does filtering rows in the Query Editor compare to AutoFilter in regular Excel?", options=["It works similarly via header dropdowns, but becomes a permanent, repeatable query step", "It's a completely different, unrelated concept", "It can only filter numbers, never text", "It requires VBA"], answer=0),
            dict(q="Why might you remove unneeded columns early in a query?", options=["It keeps the query lighter and later steps clearer", "It is required before any other step can run", "It automatically fixes data type errors", "It deletes the original source file"], answer=0),
        ],
    ),
    dict(
        id="U12C4",
        num=4,
        title="Merging Queries (Like a Lookup/Join)",
        notes=[
            "Merge Queries (Home tab in the Query Editor) combines two queries side by side based on a matching column - conceptually similar to VLOOKUP/XLOOKUP (Unit 7), but working on entire tables at once.",
            "You choose a Join Kind: Left Outer (keep everything from the first table, matched where possible) is the most common and closest to how VLOOKUP behaves.",
            "After merging, the second table appears as a single new column containing nested tables - you then expand it (a small icon on the column header) to choose which of its columns to pull in.",
            "Unlike a single VLOOKUP formula pulling one value at a time, a Merge brings in as many matched columns as you choose, for every row, in one repeatable step.",
            "This is especially useful for combining a transactions list with a separate lookup table (like Product details or Customer details) before further analysis.",
        ],
        practice=[
            "Build two small tables: one with transactions (including a Product Code column), one with product details (Product Code, Name, Price).",
            "Load both as separate queries, then use Merge Queries to combine them on Product Code, using a Left Outer join.",
            "Expand the merged column to pull in the Product Name and Price columns into your transactions query.",
            "Compare this result conceptually to what a VLOOKUP formula would have produced for the same task.",
        ],
        quiz=[
            dict(q="What does Merge Queries do?", options=["Combines two queries side by side based on a matching column", "Stacks two queries on top of each other", "Deletes duplicate queries", "Converts a query into a chart"], answer=0),
            dict(q="Which Join Kind is closest in behavior to how VLOOKUP works?", options=["Left Outer", "Right Anti", "Full Outer only", "None, they are unrelated"], answer=0),
            dict(q="After merging, how do you choose which columns from the second table to bring in?", options=["Expand the new nested-table column via its header icon", "They are all included automatically with no choice", "Use a separate VLOOKUP formula", "Delete and rebuild the query"], answer=0),
            dict(q="How is a Merge different from a single VLOOKUP formula?", options=["It brings in multiple matched columns for every row in one repeatable step, not just one value at a time", "It can only match exactly one row total", "It cannot be refreshed", "It only works within the same sheet"], answer=0),
        ],
    ),
    dict(
        id="U12C5",
        num=5,
        title="Appending Queries (Stacking Data)",
        notes=[
            "Append Queries (Home tab in the Query Editor) stacks two or more queries on top of each other, combining rows from tables with the same (or similar) column structure.",
            "This is the Power Query equivalent of manually copy-pasting one month's data below another's - except it's a repeatable step that re-runs cleanly every time you refresh.",
            "A common use case: combining several monthly sales files (Jan query, Feb query, Mar query...) that share the same columns into one long combined table for analysis.",
            "If column names don't match exactly between the queries being appended, Power Query will still stack them but may create extra columns or leave mismatches with blank values - consistent column naming beforehand avoids this.",
            "Append is different from Merge: Merge combines columns side by side (like a lookup/join); Append stacks rows on top of each other (like combining several similar lists into one).",
        ],
        practice=[
            "Build two small tables with identical column structures, representing 'January Sales' and 'February Sales'.",
            "Load both as separate queries, then use Append Queries to stack them into one combined query.",
            "Deliberately rename one column slightly differently in one of the source tables, redo the append, and observe the mismatch it creates.",
            "Fix the column name to match and confirm the append now stacks cleanly.",
        ],
        quiz=[
            dict(q="What does Append Queries do?", options=["Stacks rows from two or more queries on top of each other", "Combines columns side by side", "Deletes duplicate rows", "Sorts a query"], answer=0),
            dict(q="What is a common real-world use case for Append?", options=["Combining several monthly files with the same columns into one long table", "Looking up a single value from another table", "Formatting a column as currency", "Creating a chart"], answer=0),
            dict(q="What happens if column names don't match exactly between appended queries?", options=["It can create extra columns or leave mismatches with blank values", "Power Query blocks the append entirely", "The columns are automatically renamed to match", "Nothing, it is always ignored safely"], answer=0),
            dict(q="How is Append different from Merge?", options=["Append stacks rows; Merge combines columns side by side based on a match", "They are the exact same operation", "Append only works on numbers", "Merge can only combine two queries, Append can combine any number"], answer=0),
        ],
    ),
    dict(
        id="U12C6",
        num=6,
        title="Loading & Refreshing Data",
        notes=[
            "Close & Load (top-left of the Query Editor) finishes a query and loads its result into the workbook - either as a new worksheet Table, or a connection-only query used elsewhere (like a Merge source).",
            "Close & Load To gives more control: choose Table, PivotTable Report, or 'Only Create Connection', and choose whether to load into the existing sheet or a new one.",
            "Once loaded, Data tab > Refresh (or Refresh All) re-runs every step of the query against the current source data - this is how a query stays useful over time instead of being a one-off import.",
            "Queries Pane (Data tab > Queries & Connections) lists every query in the workbook, showing row counts and letting you double-click any query to reopen its editor and adjust steps.",
            "A query can be set to refresh automatically when the file opens (right-click the query > Properties) - useful for a workbook meant to always show current data without a manual refresh step.",
        ],
        practice=[
            "Take one of your earlier queries and use Close & Load to place its result into a new worksheet as a Table.",
            "Change some values in the original source data, then use Data > Refresh All and confirm the loaded table updates.",
            "Open Queries & Connections and locate your query, checking its row count.",
            "Right-click a query, open Properties, and locate the 'Refresh data when opening the file' option (no need to change it, just find it).",
        ],
        quiz=[
            dict(q="What does Close & Load To let you choose that plain Close & Load doesn't offer as directly?", options=["Table, PivotTable Report, or Connection-only, and where to load it", "The query's name", "The source file location", "The column data types"], answer=0),
            dict(q="What does Refresh All do?", options=["Re-runs every step of every query against current source data", "Deletes all queries", "Only refreshes charts", "Only works once per file"], answer=0),
            dict(q="Where can you see every query in a workbook along with its row count?", options=["Queries & Connections pane", "Name Manager", "Format Cells", "Conditional Formatting Manager"], answer=0),
            dict(q="Where would you set a query to refresh automatically when the file opens?", options=["Right-click the query > Properties", "Data Validation settings", "Page Layout tab", "Insert tab"], answer=0),
        ],
    ),
]

UNIT13_CHAPTERS = [
    dict(
        id="U13C1",
        num=1,
        title="What is a Macro & the Developer Tab",
        notes=[
            "A macro is a recorded or written sequence of actions that Excel can replay automatically - useful for repetitive tasks you do the same way every time.",
            "The Developer tab isn't shown by default - enable it via File > Options > Customize Ribbon, then check 'Developer' in the right-hand list.",
            "The Developer tab holds the main macro tools: Record Macro, Macros (to view/run/edit/delete them), Visual Basic (the code editor), and controls for adding buttons.",
            "Macros carry more risk than formulas: since they can perform many actions automatically (including deleting or changing data), only run macros from sources you trust.",
            "Macro-enabled workbooks use a different file extension, .xlsm, instead of the regular .xlsx - this is covered in more detail in a later chapter of this unit.",
        ],
        practice=[
            "Enable the Developer tab via File > Options > Customize Ribbon if it isn't already visible.",
            "Open the Developer tab and locate Record Macro, Macros, and Visual Basic buttons.",
            "In your own words, write down one repetitive task in your own Excel usage that a macro could potentially automate.",
            "Note why running an unfamiliar macro from an untrusted source could be risky, based on what a macro is capable of doing.",
        ],
        quiz=[
            dict(q="What is a macro?", options=["A recorded or written sequence of actions Excel can replay automatically", "A type of chart", "A built-in worksheet function", "A file format"], answer=0),
            dict(q="Where do you enable the Developer tab?", options=["File > Options > Customize Ribbon", "Data tab", "Insert tab", "View tab"], answer=0),
            dict(q="Why should you be cautious about running macros from untrusted sources?", options=["Macros can perform many automatic actions, including changing or deleting data", "Macros always corrupt the file instantly", "Macros are illegal to use", "Macros only work with numbers"], answer=0),
            dict(q="What file extension is used for macro-enabled workbooks?", options=[".xlsm", ".xlsx", ".xls", ".csv"], answer=0),
        ],
    ),
    dict(
        id="U13C2",
        num=2,
        title="Recording Your First Macro",
        notes=[
            "Developer tab > Record Macro starts recording; every click, format change, or formula you enter is captured until you press Stop Recording.",
            "Before recording, give the macro a clear name (no spaces - use underscores instead, e.g. Highlight_Totals) and optionally a shortcut key and description.",
            "Choose where to store it: 'This Workbook' keeps it tied to the current file; 'Personal Macro Workbook' makes it available in every workbook you open on your computer.",
            "Keep recorded actions simple and deliberate at first - every single click and correction gets recorded too, including mistakes, which can make the macro behave unexpectedly.",
            "Once recorded, Developer tab > Macros lets you select it and click Run to replay the exact sequence of actions on new data.",
        ],
        practice=[
            "Record a simple macro named Bold_Header that selects a header row and makes it bold, then stop recording.",
            "Go to a different sheet or a fresh set of data with a similar header row, then run your macro via Developer > Macros > Run.",
            "Record a second macro that applies a number format (like Currency) to a selected range.",
            "Deliberately make a small mistake while recording a throwaway third macro, then observe how that mistake gets replayed when you run it.",
        ],
        quiz=[
            dict(q="What happens while a macro is being recorded?", options=["Every action you take is captured until you stop recording", "Nothing happens until you press Run", "Only formulas are recorded, not formatting", "Only the final result is saved"], answer=0),
            dict(q="Where would you store a macro to make it available in every workbook you open?", options=["Personal Macro Workbook", "This Workbook only", "A new blank workbook", "The Name Manager"], answer=0),
            dict(q="Why should macro names avoid spaces?", options=["Macro names use underscores instead of spaces by convention/requirement", "Spaces make macros run slower", "Spaces are not allowed in any Excel names ever", "It is only a stylistic preference with no functional reason"], answer=0),
            dict(q="What happens if you make a mistake while recording a macro?", options=["The mistake gets recorded too and will be replayed when the macro runs", "Excel automatically skips it", "Recording stops automatically", "It gets fixed the next time you run it"], answer=0),
        ],
    ),
    dict(
        id="U13C3",
        num=3,
        title="Running Macros & Assigning Buttons",
        notes=[
            "Beyond Developer > Macros > Run, a macro can be triggered by a keyboard shortcut assigned when it was recorded (or edited later via Macro Options).",
            "A macro can be assigned to a custom button placed directly on the worksheet: Developer > Insert > Button (Form Control), draw it on the sheet, then pick the macro to link.",
            "Buttons make macros accessible to someone who wouldn't know where to find Developer > Macros - useful for a tool meant to be shared or reused by others.",
            "A macro can also be assigned to an existing shape or picture (right-click it > Assign Macro) instead of a plain Form Control button, for a more custom look.",
            "Running the same macro twice in a row can sometimes produce a different result than running it once, if the macro's actions depend on the sheet's current state - worth testing before relying on it heavily.",
        ],
        practice=[
            "Assign a keyboard shortcut to one of your recorded macros via Developer > Macros > Options.",
            "Insert a Form Control button on your sheet and assign one of your macros to it.",
            "Click the button to confirm it runs the macro correctly.",
            "Test running the same macro twice in a row on the same data and note whether the result changes the second time.",
        ],
        quiz=[
            dict(q="Besides Developer > Macros > Run, how else can a macro be triggered?", options=["A keyboard shortcut, or a button/shape assigned to it", "Only by restarting Excel", "Only by saving the file", "Macros cannot be triggered any other way"], answer=0),
            dict(q="How do you add a clickable button on the worksheet linked to a macro?", options=["Developer > Insert > Button (Form Control), then assign the macro", "Insert > Chart", "Data > Data Validation", "Page Layout > Print Area"], answer=0),
            dict(q="Why are buttons useful for macros meant to be shared with others?", options=["They make macros accessible without needing to know where Developer > Macros is", "They make the macro run faster", "They are required for macros to work at all", "They automatically document the macro's code"], answer=0),
            dict(q="Why is it worth testing whether running a macro twice produces the same result?", options=["Some macros' actions depend on the sheet's current state and could behave differently the second time", "All macros always behave identically no matter how many times they run", "It only matters for macros with buttons", "It only matters if the macro has a keyboard shortcut"], answer=0),
        ],
    ),
    dict(
        id="U13C4",
        num=4,
        title="Intro to the VBA Editor",
        notes=[
            "The Visual Basic Editor (VBE) opens via Developer > Visual Basic (or Alt+F11) - it's where macro code actually lives, whether recorded or hand-written.",
            "The Project Explorer pane (usually left side) lists every open workbook and its sheets/modules - recorded macros are stored inside a 'Module' by default.",
            "Double-clicking a Module opens its code in the main editing pane, showing the actual VBA (Visual Basic for Applications) instructions behind a recorded macro, line by line.",
            "Reading a recorded macro's generated code is a great way to learn VBA syntax by example - even without writing code from scratch, you can see how Excel described your clicks in code form.",
            "The Immediate Window (View > Immediate Window, or Ctrl+G) is a small scratchpad for testing single lines of VBA code quickly, without needing a full macro.",
        ],
        practice=[
            "Open the VBA Editor via Alt+F11 and locate the Project Explorer pane.",
            "Find the Module containing one of your recorded macros from earlier chapters and open it.",
            "Read through the generated code line by line, and try to match each line to an action you remember performing while recording.",
            "Open the Immediate Window (Ctrl+G) and type a simple line like MsgBox \"Hello\" then press Enter to run it directly.",
        ],
        quiz=[
            dict(q="What shortcut opens the Visual Basic Editor?", options=["Alt+F11", "Ctrl+F11", "Alt+F4", "Ctrl+Alt+V"], answer=0),
            dict(q="Where is a recorded macro's code stored by default?", options=["Inside a Module", "Inside a Named Range", "Inside a Pivot Table", "Inside a Slicer"], answer=0),
            dict(q="Why is reading a recorded macro's generated code useful for learning?", options=["It shows how Excel described your clicks in actual VBA syntax", "It is the only way to run a macro", "It automatically fixes errors in the macro", "It converts the macro into a formula"], answer=0),
            dict(q="What is the Immediate Window used for?", options=["Testing single lines of VBA code quickly", "Recording new macros", "Viewing Pivot Table data", "Formatting cells"], answer=0),
        ],
    ),
    dict(
        id="U13C5",
        num=5,
        title="Basic VBA: Variables & MsgBox",
        notes=[
            "A VBA procedure (a 'Sub') is written as: Sub ProcedureName() ... End Sub - everything between those lines runs when the macro executes.",
            "A variable stores a value for use later in the code, declared with Dim: Dim total As Integer declares a variable named 'total' that will hold a whole number.",
            "Common variable types: Integer/Long (whole numbers), Double (decimals), String (text), Boolean (True/False).",
            "MsgBox displays a simple popup message - useful for showing information or confirming a macro ran: MsgBox \"Done!\" or MsgBox \"Total is: \" & total to include a variable's value.",
            "Assigning a value to a variable uses =, e.g. total = 10, and that variable can then be used in later lines, including inside a MsgBox.",
        ],
        practice=[
            "In the VBA Editor, insert a new Module (right-click Project Explorer > Insert > Module) and write a new Sub.",
            "Inside it, declare an Integer variable, assign it a value, and show that value in a MsgBox.",
            "Declare a String variable holding your name, and show a MsgBox that says 'Hello, ' followed by that variable.",
            "Run your Sub directly from the VBA Editor (F5) and confirm the MsgBox appears as expected.",
        ],
        quiz=[
            dict(q="How is a VBA procedure (Sub) structured?", options=["Sub ProcedureName() ... End Sub", "Function ProcedureName() ... End Function only", "Begin ProcedureName ... Finish", "Macro ProcedureName ... EndMacro"], answer=0),
            dict(q="What keyword declares a variable in VBA?", options=["Dim", "Var", "Let", "Set"], answer=0),
            dict(q="What does MsgBox do?", options=["Displays a simple popup message", "Saves the workbook", "Opens a new sheet", "Runs a Pivot Table refresh"], answer=0),
            dict(q="Which variable type would you use to store text?", options=["String", "Integer", "Boolean", "Double"], answer=0),
        ],
    ),
    dict(
        id="U13C6",
        num=6,
        title="Basic VBA: Loops & Simple Automation",
        notes=[
            "A For loop repeats a block of code a set number of times: For i = 1 To 10 ... Next i runs the code inside once for each value of i from 1 to 10.",
            "Inside a loop, Cells(row, column) refers to a specific worksheet cell by its row and column NUMBER (not letter) - e.g. Cells(1,1) is cell A1, useful when the row number changes each loop.",
            "A common pattern: use a loop variable as the row number to process many rows the same way, e.g. For i = 2 To 10: Cells(i,1).Value = Cells(i,1).Value * 2: Next i doubles a column of values.",
            "An If statement inside VBA works much like the IF function (Unit 4), but as code: If Cells(i,1).Value > 100 Then ... End If.",
            "Combining a loop with an If inside it is a very common simple automation pattern: go through many rows, and do something only when a condition is met for that row.",
        ],
        practice=[
            "Write a Sub with a For loop from 1 to 5 that shows a MsgBox with the current loop number each time.",
            "Write a Sub that uses a loop to write the numbers 1 through 10 down column A using Cells(row, column).",
            "Extend that loop with an If statement that only writes the number if it's even.",
            "Run each Sub and confirm the results appear correctly in the worksheet.",
        ],
        quiz=[
            dict(q="What does a For loop do?", options=["Repeats a block of code a set number of times", "Runs code only once", "Deletes a range of cells", "Creates a chart"], answer=0),
            dict(q="What does Cells(1,1) refer to?", options=["Cell A1", "Cell B1", "The entire first row", "The entire first column"], answer=0),
            dict(q="Why is Cells(row, column) especially useful inside a loop?", options=["The row number can change automatically with the loop variable", "It only works outside of loops", "It is faster than any other method", "It cannot reference formulas"], answer=0),
            dict(q="What is a common pattern combining a loop and an If statement?", options=["Going through many rows and doing something only when a condition is met", "Deleting the entire worksheet", "Running the loop exactly once", "Avoiding the use of variables"], answer=0),
        ],
    ),
    dict(
        id="U13C7",
        num=7,
        title="Macro Security & Saving as .xlsm",
        notes=[
            "A workbook containing macros must be saved as .xlsm (Macro-Enabled Workbook) - saving as regular .xlsx silently strips all macros out, with a warning dialog at save time.",
            "By default, Excel blocks macros in files downloaded from the internet or received as attachments, showing a 'Security Warning' bar - only enable macros ('Enable Content') if you trust the file's source.",
            "Trust Center (File > Options > Trust Center > Trust Center Settings > Macro Settings) controls how strictly Excel blocks macros - the safest general setting disables macros with notification, letting you decide file by file.",
            "Digitally signing macros (for advanced/organizational use) lets a trusted publisher's macros run without triggering warnings - beyond this course's current scope, but good to know it exists.",
            "Good practice: only enable macros in files from people/sources you trust, and keep a plain .xlsx backup of important data separate from any macro-enabled version.",
        ],
        practice=[
            "Save one of your practice workbooks containing a macro as .xlsm and confirm the macro still works after reopening it.",
            "Try saving the same file as .xlsx instead and observe the warning about macros being removed.",
            "Open File > Options > Trust Center > Trust Center Settings > Macro Settings and read through the available options (no need to change them).",
            "Write a short note in your own words on why you'd be cautious about clicking 'Enable Content' on a macro file from an unknown source.",
        ],
        quiz=[
            dict(q="What file extension must a macro-containing workbook be saved as to keep its macros?", options=[".xlsm", ".xlsx", ".csv", ".xls"], answer=0),
            dict(q="What happens if you save a macro-containing workbook as .xlsx?", options=["The macros are silently stripped out, with a warning at save time", "The file becomes corrupted", "The macros are converted to formulas", "Nothing changes"], answer=0),
            dict(q="What does the Security Warning bar relate to?", options=["Excel blocking macros by default in files from the internet or email attachments", "A formula error", "A missing chart", "A broken hyperlink"], answer=0),
            dict(q="What is good practice regarding 'Enable Content' on a macro file?", options=["Only enable it for files from sources you trust", "Always click it immediately to see what happens", "Never use macros at all under any circumstance", "Only relevant for .csv files"], answer=0),
        ],
    ),
]

UNIT14_CHAPTERS = [
    dict(
        id="U14C1",
        num=1,
        title="Capstone Project Brief: A Small Business Tracker",
        notes=[
            "The capstone project combines most of the skills from this entire course into one connected workbook: a Small Business Tracker for a fictional shop.",
            "The project has three parts across this unit's remaining chapters: (1) data entry + formulas + lookups, (2) pivot table + chart + dashboard, (3) polish and review.",
            "Planned workbook structure: a 'Products' sheet (price list), a 'Transactions' sheet (raw sales entries, one row per sale), and a 'Dashboard' sheet (the finished summary view).",
            "Before building anything, plan the columns each sheet needs on paper or in a notes cell first - this mirrors how real spreadsheet projects are usually planned, rather than built column-by-column with no end goal in mind.",
            "There's no single 'correct' way to build this - the goal is applying formulas, lookups, cleaning, and either a pivot table or chart confidently on a project that resembles something you might actually use.",
        ],
        practice=[
            "Create a new workbook (or new sheets in your practice workbook) named Products, Transactions, and Dashboard.",
            "On the Products sheet, plan and list at least 6 products with a Product Code, Name, and Price column.",
            "On the Transactions sheet, plan the columns you'll need for each sale (hint: you'll need at least a Date, Product Code, and Quantity - more in the next chapter).",
            "Write a short plan (as a note or comment) describing what the finished Dashboard sheet should show, before you build it.",
        ],
        quiz=[
            dict(q="What are the three planned parts of the capstone project?", options=["Data entry + formulas + lookups, then pivot/chart/dashboard, then polish and review", "Only building one giant chart", "Only writing VBA macros", "Only importing data via Power Query"], answer=0),
            dict(q="What three sheets does the planned workbook structure include?", options=["Products, Transactions, Dashboard", "Home, Insert, Data", "Notes, Practice, Quiz", "Rows, Columns, Values"], answer=0),
            dict(q="Why is it suggested to plan columns before building each sheet?", options=["It mirrors how real spreadsheet projects are usually planned with an end goal in mind", "Excel requires a written plan before entering data", "It automatically generates the formulas for you", "It is required to enable macros"], answer=0),
            dict(q="What is the goal of the capstone project?", options=["Confidently applying formulas, lookups, cleaning, and a pivot table/chart on a realistic project", "Memorizing every function in Excel", "Building the largest possible dataset", "Learning a second spreadsheet program"], answer=0),
        ],
    ),
    dict(
        id="U14C2",
        num=2,
        title="Building the Capstone: Data Entry, Formulas & Lookups",
        notes=[
            "On the Transactions sheet, each row should represent one sale: Date, Product Code, Quantity, plus calculated columns for Unit Price and Total that pull from the Products sheet.",
            "Use VLOOKUP or XLOOKUP (Unit 7) on the Transactions sheet to pull each sale's Unit Price automatically from the Products sheet, based on the Product Code entered.",
            "Calculate a Total column per row: Quantity * Unit Price - this should update automatically as soon as a Quantity or Product Code changes.",
            "Apply Data Validation (Unit 5) to the Product Code column on Transactions, using a dropdown list sourced from the Products sheet, to prevent typos that would break the lookup.",
            "Wrap your lookup formulas in IFERROR (Unit 5) to show a clean message like 'Check Code' instead of #N/A if an invalid Product Code is accidentally entered.",
        ],
        practice=[
            "On Transactions, enter at least 15 sample sale rows with varied Dates, Product Codes, and Quantities.",
            "Add a Unit Price column that uses VLOOKUP/XLOOKUP to pull the price from the Products sheet automatically.",
            "Add a Total column that multiplies Quantity by the looked-up Unit Price.",
            "Add Data Validation to the Product Code column (dropdown sourced from Products) and wrap your lookup in IFERROR.",
        ],
        quiz=[
            dict(q="What does the Unit Price column on Transactions use to pull its value automatically?", options=["A lookup formula (VLOOKUP/XLOOKUP) referencing the Products sheet", "A manually typed value each time", "A Pivot Table", "A macro"], answer=0),
            dict(q="How is the Total column for each sale calculated?", options=["Quantity multiplied by the looked-up Unit Price", "Quantity plus Unit Price", "A fixed value entered manually", "The average of all sales"], answer=0),
            dict(q="What does adding Data Validation to the Product Code column help prevent?", options=["Typos that would break the lookup formula", "Slow calculation speed", "Chart errors", "Macro security warnings"], answer=0),
            dict(q="Why wrap the lookup formula in IFERROR here?", options=["To show a clean message instead of #N/A if an invalid code is entered", "To make the formula calculate faster", "It is required for VLOOKUP to function at all", "To automatically fix typos"], answer=0),
        ],
    ),
    dict(
        id="U14C3",
        num=3,
        title="Building the Capstone: Pivot Table, Chart & Dashboard Wrap-Up",
        notes=[
            "With Transactions data complete, build a Pivot Table (Unit 11) summarizing Total sales by Product and by Month (grouping the Date field).",
            "Add a Pivot Chart or regular chart (Unit 9) showing sales trends over time, or a comparison across products - choose whichever chart type tells the clearer story for your data.",
            "Bring your Pivot Table/Chart onto the Dashboard sheet, alongside a couple of key summary numbers calculated with SUMIFS/COUNTIFS (Unit 5), like Total Revenue and Number of Sales.",
            "Add a Slicer (Unit 11) connected to your Pivot Table/Chart so the Dashboard sheet is interactive, and apply consistent formatting/colors across the sheet for a polished look.",
            "Finally, review the whole workbook end to end: check formulas still work with new data added at the bottom of Transactions, confirm the Dashboard updates on Refresh, and clean up any leftover test data - this final review habit matters on any real project, not just this one.",
        ],
        practice=[
            "Build a Pivot Table summarizing Total sales by Product and by Month from your Transactions data.",
            "Add a chart (Pivot Chart or regular) visualizing this summary in whichever way tells the clearest story.",
            "Assemble the Dashboard sheet: key summary numbers, your chart, and a Slicer, all styled consistently.",
            "Add 2-3 new transaction rows at the bottom of Transactions, refresh everything, and confirm the Dashboard updates correctly end to end.",
        ],
        quiz=[
            dict(q="What does the capstone's Pivot Table summarize?", options=["Total sales by Product and by Month", "Only the number of macros used", "Only the Products sheet's prices", "Nothing, it's for formatting only"], answer=0),
            dict(q="What goes alongside the chart on the Dashboard sheet?", options=["Key summary numbers calculated with SUMIFS/COUNTIFS, and a Slicer for interactivity", "Only raw unformatted data", "A copy of the VBA code", "The Trust Center settings"], answer=0),
            dict(q="What is the purpose of the final end-to-end review step?", options=["Confirming formulas, refresh, and the dashboard all still work correctly with new data", "Deleting the Products sheet", "Converting the workbook to .xlsm", "Removing all charts before finishing"], answer=0),
            dict(q="Why does this final review habit matter beyond just this project?", options=["It's a good habit for checking any real spreadsheet project before considering it done", "It is only relevant to this specific capstone", "It replaces the need for Data Validation", "It is only needed if macros are used"], answer=0),
        ],
    ),
]

UNITS = [
    dict(num=1, title="Getting Started", chapters=UNIT1_CHAPTERS),
    dict(num=2, title="Formatting & Sheet Management", chapters=UNIT2_CHAPTERS),
    dict(num=3, title="Formula Foundations", chapters=UNIT3_CHAPTERS),
    dict(num=4, title="Logical & Text Functions", chapters=UNIT4_CHAPTERS),
    dict(num=5, title="Conditional Functions & Data Rules", chapters=UNIT5_CHAPTERS),
    dict(num=6, title="Organizing & Cleaning Data", chapters=UNIT6_CHAPTERS),
    dict(num=7, title="Lookup & Reference Functions", chapters=UNIT7_CHAPTERS),
    dict(num=8, title="Tables, Named Ranges & Dynamic Arrays", chapters=UNIT8_CHAPTERS),
    dict(num=9, title="Charts & Visualization", chapters=UNIT9_CHAPTERS),
    dict(num=10, title="What-If Analysis & Financial Basics", chapters=UNIT10_CHAPTERS),
    dict(num=11, title="Pivot Tables & Dashboards", chapters=UNIT11_CHAPTERS),
    dict(num=12, title="Power Query", chapters=UNIT12_CHAPTERS),
    dict(num=13, title="Macros & VBA", chapters=UNIT13_CHAPTERS),
    dict(num=14, title="Capstone", chapters=UNIT14_CHAPTERS),
]


def all_chapters_flat():
    """Flat, ordered list of every chapter across every unit, in course order."""
    flat = []
    for unit in UNITS:
        for ch in unit["chapters"]:
            flat.append(ch)
    return flat
